# 灵犀 (LingXi) 项目状态报告与修改建议

> 生成时间：2026-08-07
> 探索范围：`d:\UGit\cross-app-assistant` 全量代码库（9 个 workspace 成员 + overlay 独立 crate + assistant-inference 排除 crate）
> 基线 commit：`031d5965`（refactor(qq): 放弃 UIA 扫控件树猜发送者，改为读用户选区）

---

## 一、项目现状概览

### 1.1 定位

灵犀是 Windows 优先的跨应用智能选区助手（Rust + Tauri v2），当前正处在从"自带拼音输入"向"OwO 负责系统输入法、灵犀仅负责 AI 选区助手"的架构转型期。`scripts/run-dev.ps1` 已明确标注"ime-server 已退役，永不启动"，但 README 仍描述旧拼音流程。

### 1.2 架构分层

```
┌─────────────────────────────────────────────┐
│  apps/overlay (Tauri 主程序，独立 crate)     │  ← UI + 命令 + 托盘 + 热键
│  ├─ src/main.rs (49KB, 19 个命令)            │
│  └─ ui/ (index.html + app.js 26KB)          │
├─────────────────────────────────────────────┤
│  crates/assistant-windows (核心适配层)        │  ← UIA/Win32/键盘/QQ
│  └─ qq.rs (22KB) + 11 个其他模块             │
├─────────────────────────────────────────────┤
│  crates/assistant-core (平台无关抽象)         │  ← InputAdapter trait + Diff
│  └─ lib.rs + diff.rs + transform.rs (~440行) │
├─────────────────────────────────────────────┤
│  crates/assistant-inference (模型层，独立)    │  ← candle 本地 + OpenAI 兼容云端
│  └─ lib.rs (49KB)                            │
└─────────────────────────────────────────────┘
```

### 1.3 工具链分裂（重要现状）

| 组件 | 工具链 | 原因 |
|------|--------|------|
| 根 workspace（9 成员） | `stable`（GNU） | 根 `rust-toolchain.toml` pin |
| apps/overlay | `stable-x86_64-pc-windows-msvc` | Tauri 在 GNU 下会调 dlltool 链接失败 |
| crates/assistant-inference | 被排除，实际也需 MSVC | candle 传递依赖需要 |

**影响**：本地开发需用 `cargo +stable-x86_64-pc-windows-msvc build --manifest-path apps/overlay/Cargo.toml --release` 构建，不能直接 `cargo build`。

### 1.4 Git 历史

最近 5 个 commit 全部围绕 QQ 集成迭代：

| Hash | Message |
|------|---------|
| `031d5965` | refactor(qq): 放弃 UIA 扫控件树猜发送者，改为读用户选区 |
| `3b316196` | fix(qq): 读取只取右侧聊天区（rel_x>=700）+ 过滤 UI 标签和时间戳 |
| `1807b3ec` | style: cargo fmt |
| `89416b9e` | fix(ui): 自定义确认弹窗 + QQ 读取诊断日志 |
| `5a11e6f2` | fix(clippy): RangeInclusive::contains |

说明最近开发焦点完全在 QQ 消息读取的反复试错上。

---

## 二、核心功能模块状态

### 2.1 选区辅助（核心，可用）

**调用链**：全局热键 `Ctrl+Alt+Space` → `on_transform` → `WindowsAdapter::capture_selection` → 模型变换 → `write_back` → `Ctrl+Alt+Backspace` 撤销。

| 模块 | 状态 | 说明 |
|------|------|------|
| `foreground.rs` | ✅ 稳定 | 前台窗口检测 |
| `uia/mod.rs` | ✅ 稳定 | UIA 客户端封装 |
| `snapshot.rs` | ✅ 稳定 | 选区快照+校验，含焦点漂移检测 |
| `read.rs` | ✅ 稳定 | TextPattern→ValuePattern→Clipboard 三级降级 |
| `writer.rs` | ✅ 稳定 | SetValue/键盘/剪贴板写回 + 撤销 |
| `keyboard.rs` | ✅ 稳定 | 含 click_at 坐标点击 |
| `clipboard.rs` | ✅ 稳定 | 剪贴板操作，保留原内容 |
| `hotkey.rs` | ✅ 稳定 | RegisterHotKey 全局热键 |

### 2.2 QQ 集成（迭代中，有已知问题）

**当前流程**：用户在 QQ 里选中消息文字 → 点灵犀"读取" → `capture_qq_selection_text` 遍历 QQ 窗口 UIA 树找 TextPattern selection → 生成草稿 → `qq_write_draft` 坐标点击+粘贴写入。

| 功能 | 状态 | 问题 |
|------|------|------|
| 读取选区 | ⚠️ 刚改 | 新方案（遍历 UIA 树找 TextPattern selection）未充分验证 |
| 生成草稿 | ✅ 可用 | 走 ModelBackend |
| 写入草稿 | ⚠️ 脆弱 | 坐标硬编码（窗口 60%x, 85%y），不同分辨率/窗口尺寸可能偏移 |
| 前台检测 | ✅ 已修 | 用 `resolve_qq_window` 兜底 remembered 窗口 |

### 2.3 模型推理（可用但配置重）

| 后端 | 状态 | 说明 |
|------|------|------|
| 本地 candle Qwen2.5-1.5B | ⚠️ 重 | 首次需下载 ~1GB GGUF，CPU 推理慢 |
| 云端 OpenAI 兼容 | ✅ 可用 | 手动 ureq + native-tls，支持任意兼容端点 |

### 2.4 已退役模块

| 模块 | 状态 | 处理建议 |
|------|------|----------|
| `apps/ime-server` | 退役 | 从 workspace 移除或移到 `archived/` |
| `apps/ime-repl` | 退役 | 同上 |
| `crates/assistant-ime` | 退役 | 同上 |
| `plugins/lingxi-rime-filter` | 实验 | 保留但在 README 注明实验性 |

---

## 三、问题清单（按优先级）

### P0 — 功能正确性

#### 3.1 QQ 选区读取可靠性未验证

**位置**：`crates/assistant-windows/src/qq.rs` `capture_qq_selection_text()`

**问题**：新方案遍历整个 QQ 窗口 UIA 树（上限 4096 元素）找有 TextPattern 且 GetSelection 非空的元素。QQNT 是 Chromium，UIA 树可能很大且虚拟化，`descendants()` 取不全。另外多个 contenteditable 可能都有 selection 语义。

**建议**：
1. 添加诊断日志：遍历时打印每个有 TextPattern 的元素的 control type / name / bounding rect / selection 长度，定位实际匹配到哪个元素
2. 如果遍历不可靠，退回"全局热键触发"方案——用 `Ctrl+Alt+Space` 同款热键，在 QQ 仍是前台时捕获，避免焦点切换问题
3. 考虑用 `ElementFromPoint` 配合鼠标位置定位用户正在交互的消息气泡

#### 3.2 qq_write_draft 坐标硬编码脆弱

**位置**：`crates/assistant-windows/src/qq.rs` `qq_write_draft()` line ~163

```rust
let click_x = rect.left + width * 6 / 10;
let click_y = rect.top + height * 85 / 100;
```

**问题**：6/10 和 85/100 是基于某次实测绘图推断的，不同 QQ 版本/窗口尺寸/DPI 缩放下会偏移。

**建议**：
1. 改为从 webview root rect 派生后扫描底部最近的 contenteditable ListItem（UIA 可见即可点击）
2. 或用 `SendInput` 点击后用 `GetGUIThreadInfo` 验证 caret 是否真的落在 composer 里，失败则重试不同坐标
3. 至少把 6/10、85/100 提为常量并注释测绘依据

### P1 — 架构整洁度

#### 3.3 qq.rs 文件过大且职责混杂（22KB）

**位置**：`crates/assistant-windows/src/qq.rs`

**问题**：单文件包含：窗口管理（LAST_QQ_HWND/resolve_qq_window）、选区捕获（capture_qq_selection_text）、写入（qq_write_draft）、编辑器选择（choose_editor/editor_score）、废弃的消息扫描（is_message_candidate/join_trailing_message/qq_latest_message）。

**建议**：拆分为
- `qq/window.rs` — 窗口解析与 remembered 状态
- `qq/selection.rs` — 选区捕获
- `qq/composer.rs` — 写入与编辑器选择
- `qq/legacy.rs` — 废弃函数（或直接删除）

#### 3.4 废弃代码未清理

**位置**：`crates/assistant-windows/src/qq.rs`

以下函数标记 `#[allow(dead_code)]` 且不再使用：
- `qq_latest_message()`（line 101，已废弃 stub）
- `is_message_candidate()`（line 263）
- `join_trailing_message()`（line ~230）
- `element_text()` 等辅助函数

**建议**：直接删除。这些是 UIA 扫消息方案的遗留物，已被用户选区方案取代，留着只会增加维护负担和误解风险。如果担心回退，用 git 历史保留即可。

#### 3.5 ime-server 相关退役未落地

**位置**：根 `Cargo.toml` workspace members 仍含 `apps/ime-server`、`apps/ime-repl`、`crates/assistant-ime`

**问题**：`run-dev.ps1` 已标注退役，但代码仍在 workspace 里，CI 会构建它们。

**建议**：
1. 短期：在 README 顶部加"架构转型中"说明，标注哪些模块退役
2. 中期：将退役模块移到 `archived/` 目录并从 workspace 移除，或直接删除分支保留

### P2 — 安全与健壮性

#### 3.6 CSP 完全关闭

**位置**：`apps/overlay/tauri.conf.json` line 47: `"csp": null`

**问题**：Tauri 应用关闭 CSP 意味着 WebView 可加载任意远程脚本，存在 XSS 注入风险（虽然当前 UI 是本地的，但模型输出可能注入 HTML）。

**建议**：设置最小 CSP，如 `"csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'"`。注意 Tauri 2 的 invoke 需要 `ipc:` 指令，参考官方文档。

#### 3.7 capabilities 未声明 tray-icon 权限

**位置**：`apps/overlay/capabilities/default.json`

**问题**：`tauri.conf.json` 启用了 tray-icon feature，`install_tray` 在 setup 里调用，但 capabilities 只声明了 `core:default/core:event/core:window`，没有 `tray-icon` 相关权限。

**建议**：在 capabilities 中添加 `"tray-icon:default"`（或 Tauri 2 实际要求的权限名）。虽然当前可能"碰巧能用"（setup 阶段绕过），但不保证升级后仍可用。

#### 3.8 assistant-inference 模型下载 flush 错误被吞

**位置**：`crates/assistant-inference/src/lib.rs` line 632: `out.flush().ok()`

**问题**：下载 GGUF 文件后 flush 错误被静默忽略，可能导致 rename 前文件未完全落盘。

**建议**：改为 `out.flush().map_err(|e| anyhow::anyhow!("flush failed: {e}"))?`，让下载失败显式报错。

### P3 — CI/CD 与构建

#### 3.9 CI 工具链可能不匹配

**位置**：`.github/workflows/ci.yml`

**问题**：CI 用 `dtolnay/rust-toolchain@stable`，在 Windows runner 上默认 host triple 可能是 MSVC 也可能是 GNU。overlay 有嵌套 `rust-toolchain.toml` pin MSVC，但 workspace job 用的是根的 GNU pin，可能因缺 dlltool 失败。

**建议**：
1. workspace job 显式加 `target: x86_64-pc-windows-gnu`
2. overlay job 显式加 `target: x86_64-pc-windows-msvc`
3. 或统一全部用 MSVC（根 rust-toolchain.toml 也改成 msvc，前提是 GNU 特有代码不存在——需验证）

#### 3.10 assistant-inference 未被 CI 覆盖

**位置**：`crates/assistant-inference` 被 workspace exclude，CI 也没有单独 job

**建议**：在 ci.yml 加第三个 job 专门构建+测试 assistant-inference（用 MSVC target）。

#### 3.11 JS 检查不完整

**位置**：`.github/workflows/ci.yml` overlay job 只 `node --check ui/ime.js`

**问题**：`ui/app.js`（26KB，主逻辑）和 `ui/pet.js` 没被语法检查。

**建议**：改为 `node --check ui/*.js` 或显式列出三个文件。

#### 3.12 CI 缺少 cargo build/check 步骤

**问题**：只跑 fmt+test+clippy，没有 `cargo build` 或 `cargo check` 验证编译。

**建议**：在 clippy 后加 `cargo build --workspace --release`（或至少 `cargo check`）。

### P4 — 代码质量

#### 3.13 app.js 单文件过大（26KB）

**位置**：`apps/overlay/ui/app.js`

**建议**：按功能拆分为 `qq.js`、`transform.js`、`settings.js`、`common.js`，用 ES module 或 IIFE 组织。当前单文件维护成本高。

#### 3.14 大量 eprintln 诊断日志

**位置**：`qq.rs` choose_editor 等

**问题**：用 `eprintln!` 打调试日志，Release 构建也会输出到 stderr，无级别控制。

**建议**：引入 `log` crate + `env_logger`，或用条件编译 `#[cfg(debug_assertions)]` 包裹诊断输出。

#### 3.15 README 与实际状态严重不符

**位置**：`README.md`

**问题**：仍描述 ime-server 启动流程、拼音输入功能，但实际已退役。

**建议**：重写 README，聚焦当前实际能力（选区 AI 助手 + QQ 草稿 + 桌宠），把拼音输入历史归到"历史架构"章节。

#### 3.16 缺少 CLAUDE.md

**问题**：`always_applied_workspace_rules` 提到"看 /data/workspace/multica/CLAUDE.md"，但灵犀项目本身没有 CLAUDE.md。

**建议**：创建 `CLAUDE.md` 记录：工具链分裂现状、overlay 构建命令、qq.rs 当前方案与已知问题、退役模块清单。供 AI 协作时快速对齐。

---

## 四、修改建议优先级路线图

### 第一阶段：稳定 QQ 集成（本周）

1. **验证 `capture_qq_selection_text` 实际效果**——在 QQ 里选消息后点读取，加诊断日志确认是否匹配到正确元素
2. **若不可靠，改用全局热键触发选区捕获**（QQ 在前台时捕获，避免焦点问题）
3. **清理 qq.rs 废弃函数**（P1-3.4）

### 第二阶段：架构整洁（2 周内）

4. **拆分 qq.rs**（P1-3.3）
5. **退役 ime-server 相关模块**（P1-3.5）
6. **重写 README + 创建 CLAUDE.md**（P4-3.15/3.16）

### 第三阶段：安全加固（1 月内）

7. **设置 CSP**（P2-3.6）
8. **补全 capabilities tray-icon 权限**（P2-3.7）
9. **修复 flush 错误吞掉**（P2-3.8）

### 第四阶段：CI 完善（1 月内）

10. **修复 CI 工具链配置**（P3-3.9）
11. **补全 assistant-inference CI**（P3-3.10）
12. **完善 JS 检查 + 加 cargo build**（P3-3.11/3.12）

### 第五阶段：前端重构（后续）

13. **拆分 app.js**（P4-3.13）
14. **引入结构化日志**（P4-3.14）

---

## 五、测试覆盖现状

| Crate | 测试数 | 覆盖情况 |
|-------|--------|----------|
| assistant-core | 11 | ✅ 良好（管线、diff、transform、unicode、焦点漂移） |
| assistant-inference | 8 | ⚠️ 中（纯函数测试，无模型推理/mock 测试） |
| assistant-windows | 少量 | ❌ 不足（qq.rs 仅 editor_score/is_message_candidate 单元测试，无集成测试） |
| overlay | 0 | ❌ 无（Tauri 命令无测试） |

**建议**：
- assistant-windows：为 `resolve_qq_window` 的 fallback 逻辑、`choose_editor` 的排序加测试（用 mock UIA 元素）
- assistant-inference：加 `ModelBackend` mock 测试验证 `complete` 错误传播
- overlay：对关键命令（capture_qq_selection、apply_transform）加集成测试或至少加 `#[cfg(test)]` 的纯逻辑测试

---

## 六、结论

灵犀项目核心选区辅助链路（capture→transform→write→undo）设计扎实、测试良好。当前主要债务集中在 **QQ 集成的反复试错遗留**和 **架构转型期文档/模块未清理**。建议按上述路线图先稳定 QQ 功能、清理退役代码、再逐步补安全与 CI。工具链分裂（GNU/MSVC 双轨）是结构性约束，短期接受、长期可考虑统一到 MSVC。
