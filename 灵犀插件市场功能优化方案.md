# 灵犀插件市场功能优化方案（下载 × 删除，完整管理闭环）

> **For agentic workers:** REQUIRED SUB-SKILL: 使用 writing-plans 技能执行本计划。逐 Task 推进，Task 内按复选框步骤顺序执行，不得跳步；每完成一步勾选对应复选框；遇到与本文档描述不符的现状代码时，停下核对，先修正文档再继续。所有文件路径均相对 `LingXi-DesktopAgent/` 仓库根。

## 计划元信息

- **Goal**：把主面板工具页里「即将开放」的插件市场横幅，升级为可用的市场页：用户可浏览皮肤包清单、点击「下载」完成安装、在「已安装」页签删除不需要的皮肤——形成「浏览 → 下载 → 安装 → 使用 → 删除」的完整管理闭环。
- **Architecture**：复用现有皮肤包体系作为 V1 唯一插件类型。皮肤目录拆为双源：**内置源**（`ui/assets/skins/`，随安装包分发、只读、不可删）与**用户源**（`%APPDATA%/lingxi/skins/`，市场下载落位点、可删）。市场目录同样双源：内置 `ui/assets/market/market.json`（保证离线可演示）+ 设置项 `market_url`（远程清单，按 id 去重合并）。新增 3 条 Tauri 命令（`market_list` / `market_install` / `market_uninstall`）与一个市场页视图；下载安装链为「条目校验 → ureq 下载 .part → sha256 校验 → 安全解压 → 清单校验 → 同卷改名 → 失败回滚」。
- **Tech Stack**：Rust（Tauri v2 命令层，新增依赖 `ureq 2` / `zip 2(deflate)` / `sha2 0.10`）；原生 JS（沿用现有 `invoke` + `showView` 视图切换模式，无新框架）。

---

## 一、现状与差距

### 1.1 现状（逐条对应代码）

| # | 现状 | 代码位置 |
|---|------|----------|
| 1 | 市场入口是 `disabled` 的横幅按钮，文案「即将开放」，点击无响应 | `apps/overlay/ui/index.html`（`#tools-market-btn` 横幅） |
| 2 | 前端没有任何市场相关代码（`app.js` 中 grep "market" 零命中） | `apps/overlay/ui/app.js` |
| 3 | 已有完整皮肤包体系：`skin.json` 清单、四状态校验、热切换、`pet-config-changed` 广播 | `apps/overlay/src/pet_skin.rs`、`pet.rs` |
| 4 | 皮肤目录只有一个：安装目录 `ui/assets/skins/`（exe 向上祖先查找），无用户可写目录 | `pet_skin.rs::skins_dir()` |
| 5 | 设置结构为结构级 `#[serde(default)]`，新增字段对旧配置文件安全 | `apps/overlay/src/settings.rs`（`BackendSettings`） |
| 6 | `Cargo.toml` 无 HTTP 客户端、无 zip 库、无哈希库 | `apps/overlay/Cargo.toml` |

### 1.2 差距（对照闭环）

「浏览 → 下载 → 安装 → 使用 → 删除」链条上的缺口：

- **可下载**：❌ 无市场清单数据源、无下载安装命令、无用户可写皮肤目录
- **可管理**：❌ 无法区分内置/用户皮肤（`PetSkinInfo` 无来源字段），无法判断某皮肤可否删除
- **可删除**：❌ 无卸载命令；且当前唯一皮肤目录随包分发，直接删会破坏安装完整性

### 1.3 结论与范围

- **V1 市场商品 = 皮肤包**：这是现有代码中唯一有实体格式、校验与热切换能力的「插件」形态；小工具/新能力类插件留待 V2 扩展清单格式。
- 需补齐三件事：**用户皮肤目录**（下载落位点）、**3 条市场命令**（列表/安装/卸载）、**市场页 UI**（两页签 + 按钮状态机）。
- 明确不做（V1）：自动更新检查、下载进度百分比、账号/积分、非皮肤类插件、断点续传。

---

## 二、「可下载」行为定义

「可下载」不是「按钮能点」，而是以下验收点全部成立。开发完成后逐条对照即为验收。

### 2.1 前置校验（点击下载后立即执行，任一失败即报错终止）

1. 条目 `id` 必须匹配 `[A-Za-z0-9_-]+`（与 `valid_skin_id` 同规则，防路径穿越）。
2. 条目 `url` 必须以 `https://` 或 `http://` 开头（http 仅用于本地演示，见第七章安全说明）。
3. 条目 `sha256` 必须是 64 位十六进制字符串，作为下载后完整性依据。
4. 安装目标若与内置皮肤同 `id` → 拒绝安装，报「与内置皮肤冲突」。

### 2.2 下载与校验（`market_install` 内部固定顺序）

1. 在用户皮肤目录下创建临时目录 `.tmp-<id>-<毫秒时间戳>`（与最终目录同卷，保证 rename 原子）。
2. 下载 zip 到 `<临时目录>/download.zip`：超时 30 秒、最多 3 次重定向；下载流边写盘边计算 sha256。
3. 下载完成后比对 sha256 与条目声明值，不一致 → 清理临时目录，报「文件校验失败」。
4. 解压限额：最多 200 个文件、累计解压 50 MB（防 zip 炸弹），超限即中止。
5. 解压安全：每个条目经 `enclosed_name` 校验防 zip-slip；文件扩展名黑名单 `exe/dll/bat/cmd/ps1/vbs/scr/msi/com` 拒绝解出。
6. 解压后必须在临时目录根部找到 `skin.json`，并复用皮肤清单校验（四状态齐全、id 一致、name 非空、帧数据合法）。
7. 清单内的 `id` 与市场条目 `id` 不一致 → 报「清单与条目不符」。

### 2.3 落地与生效

1. 全部校验通过 → 临时目录改名为用户皮肤目录下的 `<id>`。
2. 升级场景（用户目录已存在同 `id`）：先把旧目录改名 `.old-<id>`，新目录就位后再删除 `.old-<id>`；中途失败则把 `.old-<id>` 移回，保持旧版本可用。
3. 任何一步失败 → 删除临时目录（及必要的 `.old-<id>` 还原），返回中文错误信息；已安装版本保持不变。
4. 安装成功且该 `id` 正是当前使用中的皮肤（升级场景）→ 重新加载视图并广播 `pet-config-changed`，桌宠立即换新版本。

### 2.4 下载过程的用户可见状态

| 阶段 | 按钮显示 | 按钮可用性 |
|------|----------|------------|
| 空闲未安装 | 下载 | 可点 |
| 下载/安装进行中 | 下载中… | 禁用（防重复点击） |
| 安装成功 | 已安装 | 禁用 |
| 安装失败 | 恢复点击前状态 | 可点；页面顶部错误条显示中文原因 |
| 已安装且有更新 | 更新 | 可点（走同一安装链，覆盖用户目录） |

---

## 三、「可删除」行为定义

### 3.1 什么能删

1. **仅用户源皮肤可删**：条目 `source === "user"` 才显示「删除」；内置皮肤（`source === "builtin"`）显示「内置」徽章，无删除按钮。
2. 删除动作 = `std::fs::remove_dir_all` 移除整个用户皮肤目录 `user_skins_dir/<id>`，不碰安装目录。

### 3.2 删除时的保护

1. **使用中保护**：待删皮肤正是当前使用中的皮肤时，先自动切换回默认皮肤 `lingxi-hamster`（复用 `set_pet_skin` 的加载 + 持久化 + `pet-config-changed` 广播逻辑），成功后再删目录；切换失败则中止删除。
2. **确认交互**：点击「删除」先弹 `confirm()`，文案含皮肤名：「删除「<name>」？下载的皮肤文件将被移除。」
3. **命令层兜底**：`market_uninstall` 收到内置源 `id` 时直接返回错误「内置皮肤不可删除」（防止前端被绕过）。

### 3.3 删除后

1. 「已安装」页签立即刷新，条目消失。
2. 「市场」页签中该条目按钮从「已安装」恢复为「下载」。
3. 若触发了回落默认皮肤，桌宠立即显示默认皮肤（事件广播已覆盖）。

---

## 四、完整流程：从浏览到卸载

### 4.1 主流程（用户视角）

1. 打开主面板 → 工具页 → 点击市场横幅 → 进入市场页（默认落在「市场」页签）。
2. 前端调 `market_list`：Rust 侧合并内置 + 远程清单、标注每条的安装状态与来源，返回条目数组 → 渲染卡片网格。
3. 点「下载」→ 按钮进入「下载中…」→ `market_install` 走 2.2 安装链 → 成功后按钮变「已安装」。
4. 切到「已安装」页签 → 展示全部皮肤（内置 + 用户），来源徽章区分；内置显示「内置」，用户显示「删除」。
5. 点用户皮肤「删除」→ `confirm()` 确认 → `market_uninstall`（含使用中回落默认皮肤）→ 列表即时刷新。
6. 回「市场」页签，该条目按钮恢复为「下载」（可重新获取）。

### 4.2 条目按钮状态机

```
NOT_INSTALLED --点击下载--> INSTALLING --成功--> INSTALLED
NOT_INSTALLED --点击下载--> INSTALLING --失败--> NOT_INSTALLED（错误条提示）
INSTALLED    --检测到新版本--> UPDATABLE --点击更新--> INSTALLING（成功后回到 INSTALLED）
UPDATABLE    --更新失败--> UPDATABLE（旧版本保留可用）
INSTALLED(user)   --点击删除--> CONFIRM --确认--> UNINSTALLING --成功--> NOT_INSTALLED
INSTALLED(builtin) --固定「内置」徽章，无删除路径
```

状态判定规则（前端依据 `market_list` 返回值计算）：

- `NOT_INSTALLED`：条目 `id` 不存在于任何皮肤源
- `INSTALLED`：`installed_version === entry.version`
- `UPDATABLE`：`version_rank(installed_version) < version_rank(entry.version)`

### 4.3 命令时序（前端 ↔ Rust）

| 命令 | 入参 | 返回 | 时机 |
|------|------|------|------|
| `market_list` | 无 | `MarketItem[]`（裸数组） | 进入市场页、安装/删除成功后刷新 |
| `market_install` | `{ id }` | `Ok(())` / `Err(中文原因)` | 点「下载/更新」；同步长任务 |
| `market_uninstall` | `{ id }` | `Ok(())` / `Err(中文原因)` | confirm 通过后 |

事件：`pet-config-changed`（已有，删除使用中皮肤回落时复用）；V1 不做进度百分比，安装中状态由前端按钮固定文案「下载中…」承载。

---

## 五、技术设计

### 5.1 皮肤双源目录

- **内置源**：现有 `skins_dir()`（exe 向上祖先查找 `ui/assets/skins/<DEFAULT>/skin.json` 所在目录），只读，随包分发。
- **用户源**：`user_skins_dir()` = `dirs::config_dir()/lingxi/skins`（Windows 即 `%APPDATA%\lingxi\skins`），市场下载落位点；目录不存在时在使用时创建。
- **查找顺序**：用户源优先于内置源；同 `id` 时用户版本生效（正常流程不会出现——市场拒装内置同 id，此规则只防御手工放置的冲突）。
- `skin_dir(id)` 升级为返回 `(PathBuf, &'static str)`；新增 `skin_source(id) -> Option<&'static str>` 供卸载与列表使用。

### 5.2 市场清单格式（market.json）

```json
{
  "entries": [
    {
      "id": "lingxi-hamster-night",
      "name": "仓鼠·夜色",
      "author": "lingxi",
      "version": "1.0.0",
      "description": "深色配色的仓鼠皮肤包",
      "url": "https://cdn.example.com/lingxi/lingxi-hamster-night-1.0.0.zip",
      "sha256": "64位十六进制",
      "size": 204800
    }
  ]
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | ✅ | `[A-Za-z0-9_-]+`，与皮肤目录名一致 |
| `name` / `author` / `version` / `description` | ✅ | 展示字段，原样透传卡片 |
| `url` | ✅ | zip 包直链，`https://` 或 `http://` |
| `sha256` | ✅ | zip 原文件哈希，64 位十六进制 |
| `size` | ❌ | 字节数，仅展示 |

### 5.3 清单合并规则

- 数据源：内置 `ui/assets/market/market.json`（离线兜底、可演示）+ `settings.market_url`（远程，运营方维护）。
- 远程拉取成功 → 按 `id` 去重、**远程条目优先**（运营更新覆盖内置兜底）。
- 远程未配置 / 拉取失败 / 解析失败 → 静默降级为仅内置清单（记日志，不报错弹窗）。
- 单条字段非法（id/url/sha256 格式不对）→ 只丢弃该条目并记日志，不影响其余条目。

### 5.4 新增依赖（Cargo.toml）

```toml
ureq = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
sha2 = "0.10"
```

选型理由：`ureq` 为同步轻量 HTTP 客户端，不引入 tokio 生态 HTTP 栈（本项目 tokio 仅 `rt-multi-thread` + `time`）；`zip` 只开 `deflate` 特性减小体积；`sha2` 纯 Rust 无系统依赖。三个依赖均无 C 编译链要求，MSVC 工具链直接可用。

### 5.5 安全边界汇总

| 威胁 | 缓解措施 |
|------|----------|
| zip-slip 路径穿越 | 每个解压条目 `enclosed_name` 校验 |
| 恶意条目 `id` 路径穿越 | `valid_skin_id` 白名单 `[A-Za-z0-9_-]+` |
| 压缩包夹带可执行载荷 | 扩展名黑名单：`exe/dll/bat/cmd/ps1/vbs/scr/msi/com` |
| zip 炸弹 | 限额：最多 200 文件、累计解压 50 MB |
| 传输篡改 | sha256 强校验，不一致即拒装 |
| 内置皮肤被覆盖/删除 | 内置同 `id` 拒装；卸载命令限定 user 源且做 `user_skins_dir` 前缀检查 |
| http 明文下载 | http 仅建议本地演示使用；sha256 保证内容完整性，正式运营清单应使用 https |

### 5.6 版本比较 `version_rank`

不引入 semver crate（避免新依赖面）：按 `['.', '-', '+']` 切分，每段 `parse::<u64>()` 失败记 0，得到 `Vec<u64>` 后逐位比较，短版本补 0 对齐。满足 `1.2.10 > 1.2.9`、`2.0.0-beta` 与 `2.0.0` 视为相等（V1 接受，运营侧避免发布同名 beta）。

### 5.7 设置新增

`BackendSettings` 增加 `market_url: String`（`Default` 为空串）。结构级 `#[serde(default)]` 已保证旧配置文件缺字段时可正常加载。

### 5.8 新增命令与注册

`market_list` / `market_install` / `market_uninstall` 三个 `#[tauri::command]`，追加进 `main.rs` 的 `invoke_handler` 注册表；`main.rs` 增加 `mod market;`。

---

## Task 1：新增依赖与 market 模块骨架（清单模型 + 纯函数 + 单元测试）

**Files**：`apps/overlay/Cargo.toml`、`apps/overlay/src/main.rs`、`apps/overlay/src/market.rs`（新建）

- [ ] 1. `apps/overlay/Cargo.toml`：在 `[dependencies]` 段内追加三行（位置任意，TOML 依赖无序）：

```toml
ureq = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
sha2 = "0.10"
```

- [ ] 2. `apps/overlay/src/main.rs`：在 `mod pet_skin;` 声明之后加一行模块声明：

```rust
mod market;
```

- [ ] 3. 新建 `apps/overlay/src/market.rs`，写入以下全部内容（本 Task 只放模型与纯函数，命令在 Task 3-5 逐步加入）：

```rust
//! 插件市场：清单获取/合并、皮肤包下载安装与卸载。
//!
//! V1 市场商品 = 皮肤包。清单双源：内置 `ui/assets/market/market.json`
//! （离线兜底、可演示）+ 设置项 `market_url`（远程清单，按 id 远程优先）。
//! 安装目标为用户皮肤目录（见 pet_skin::user_skins_dir），内置皮肤不可覆盖/删除。

use serde::{Deserialize, Serialize};
use std::path::PathBuf;

use crate::pet_skin;

/// zip 下载与解压限额：最多文件数。
const MAX_FILES: usize = 200;
/// zip 下载与解压限额：累计解压字节数（50 MB）。
const MAX_UNCOMPRESSED: u64 = 50 * 1024 * 1024;
/// 解压时拒绝的扩展名（小写、无点）。
const BLOCKED_EXTS: [&str; 9] = ["exe", "dll", "bat", "cmd", "ps1", "vbs", "scr", "msi", "com"];

/// 市场清单条目（market.json 的 `entries` 元素）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MarketEntry {
    pub id: String,
    pub name: String,
    #[serde(default)]
    pub author: String,
    pub version: String,
    #[serde(default)]
    pub description: String,
    pub url: String,
    pub sha256: String,
    #[serde(default)]
    pub size: Option<u64>,
}

/// 市场清单文件结构。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MarketCatalog {
    #[serde(default)]
    pub entries: Vec<MarketEntry>,
}

/// 发给前端的条目：清单字段 + 本机安装状态。
#[derive(Debug, Clone, Serialize)]
pub struct MarketItem {
    pub id: String,
    pub name: String,
    pub author: String,
    pub version: String,
    pub description: String,
    pub size: Option<u64>,
    /// 已安装来源："builtin" | "user"；未安装为空串。
    pub installed_source: String,
    /// 已安装版本；未安装为空串。
    pub installed_version: String,
    /// true = 本机已有更低版本，按钮应显示「更新」。
    pub updatable: bool,
}

/// 内置清单定位：内置皮肤根（ui/assets/skins）的上级目录下 market/market.json。
pub fn builtin_catalog_path() -> Option<PathBuf> {
    pet_skin::skins_dir()
        .and_then(|skins| skins.parent().map(|assets| assets.join("market").join("market.json")))
}

/// 版本 → 可比较序列：按 . - + 切分，每段解析 u64，失败记 0。
pub fn version_rank(version: &str) -> Vec<u64> {
    version
        .split(['.', '-', '+'])
        .map(|part| part.trim().parse::<u64>().unwrap_or(0))
        .collect()
}

/// candidate 是否严格比 current 新（逐位比较，缺位补 0，相等视为不新）。
pub fn version_newer(candidate: &str, current: &str) -> bool {
    let (a, b) = (version_rank(candidate), version_rank(current));
    let len = a.len().max(b.len());
    for i in 0..len {
        let x = a.get(i).copied().unwrap_or(0);
        let y = b.get(i).copied().unwrap_or(0);
        if x != y {
            return x > y;
        }
    }
    false
}

/// sha256 字段合法性：64 位十六进制。
pub fn valid_sha256(value: &str) -> bool {
    value.len() == 64 && value.chars().all(|c| c.is_ascii_hexdigit())
}

/// 下载地址合法性：仅允许 http/https。
pub fn valid_url(url: &str) -> bool {
    url.starts_with("https://") || url.starts_with("http://")
}

/// 单条清单校验：id/name/url/sha256 任一非法即丢弃该条目。
pub fn sanitize_entry(entry: MarketEntry) -> Option<MarketEntry> {
    if !pet_skin::valid_skin_id(&entry.id) {
        eprintln!("[lingxi] market: 丢弃非法 id 条目：{}", entry.id);
        return None;
    }
    if entry.name.trim().is_empty() {
        eprintln!("[lingxi] market: 丢弃缺少 name 的条目：{}", entry.id);
        return None;
    }
    if !valid_url(&entry.url) {
        eprintln!("[lingxi] market: 丢弃非法 url 的条目：{}", entry.id);
        return None;
    }
    if !valid_sha256(&entry.sha256) {
        eprintln!("[lingxi] market: 丢弃非法 sha256 的条目：{}", entry.id);
        return None;
    }
    Some(entry)
}

#[cfg(test)]
mod tests {
    use super::*;

    fn demo_entry() -> MarketEntry {
        MarketEntry {
            id: "demo-skin".into(),
            name: "演示皮肤".into(),
            author: "lingxi".into(),
            version: "1.0.0".into(),
            description: String::new(),
            url: "https://example.com/demo-skin-1.0.0.zip".into(),
            sha256: "0".repeat(64),
            size: Some(1024),
        }
    }

    #[test]
    fn version_rank_orders() {
        assert!(version_newer("1.2.10", "1.2.9"));
        assert!(!version_newer("1.2.9", "1.2.10"));
        assert!(!version_newer("1.0.0", "1.0.0"));
        assert!(version_newer("2.0", "1.9.9"));
        assert!(!version_newer("1.0", "1.0.0"));
        assert!(version_newer("1.0.1", "1.0"));
    }

    #[test]
    fn sanitize_rejects_bad_entries() {
        assert!(sanitize_entry(demo_entry()).is_some());
        assert!(sanitize_entry(MarketEntry { id: "../evil".into(), ..demo_entry() }).is_none());
        assert!(sanitize_entry(MarketEntry { id: String::new(), ..demo_entry() }).is_none());
        assert!(sanitize_entry(MarketEntry { url: "ftp://x/a.zip".into(), ..demo_entry() }).is_none());
        assert!(sanitize_entry(MarketEntry { sha256: "abc".into(), ..demo_entry() }).is_none());
        assert!(sanitize_entry(MarketEntry { name: "  ".into(), ..demo_entry() }).is_none());
    }
}
```

- [ ] 4. 验证（在 `apps/overlay` 目录执行）：

```bash
cargo test
cargo build
```

预期：`version_rank_orders` 与 `sanitize_rejects_bad_entries` 两个测试通过，`cargo build` 无错误。若 `zip` 依赖特性报错，确认 Cargo.toml 使用的是 `default-features = false, features = ["deflate"]` 写法。

---

## Task 2：皮肤双源改造（用户目录 + 来源标识 + asset 图片通路）

**Files**：`apps/overlay/src/pet_skin.rs`、`apps/overlay/tauri.conf.json`

改造要点：①新增用户皮肤目录 `%APPDATA%/lingxi/skins`，查找时用户源优先；②`PetSkinInfo` 增加 `source` 字段；③用户皮肤图片经 Tauri asset protocol 提供 URL（CSP 的 `img-src` 已允许 `http://asset.localhost`，仅需开启 `assetProtocol`）；④清单校验参数化，供安装链在落地前复用。

- [ ] 1. `apps/overlay/tauri.conf.json`：`app.security` 块内、`"csp"` 行之后追加（csp 值原样不动）：

```json
"assetProtocol": {
  "enable": true,
  "scope": ["$APPDATA/lingxi/skins/**"]
}
```

- [ ] 2. `pet_skin.rs` 头部 use 区替换（原 `use std::path::PathBuf;` 一行改为）：

```rust
use std::path::{Path, PathBuf};
```

- [ ] 3. `pet_skin.rs`：在 `DEFAULT_SKIN_ID` / `PET_STATES` 常量之后新增：

```rust
/// 皮肤来源标识：内置（安装包内，不可删）。
pub const SKIN_SOURCE_BUILTIN: &str = "builtin";
/// 皮肤来源标识：用户目录（市场下载落位点，可删）。
pub const SKIN_SOURCE_USER: &str = "user";

/// 用户皮肤目录：`%APPDATA%/lingxi/skins`（市场下载落位点，可删除）。
pub fn user_skins_dir() -> Option<PathBuf> {
    dirs::config_dir().map(|dir| dir.join("lingxi").join("skins"))
}
```

- [ ] 4. `pet_skin.rs`：`PetSkinInfo` 增加来源字段（新增最后一行）：

```rust
pub struct PetSkinInfo {
    pub id: String,
    pub name: String,
    pub author: String,
    pub version: String,
    pub description: String,
    /// 内置皮肤为相对 `ui/` 的缩略图 URL；用户皮肤为 asset protocol URL（spritesheet 带 `#row`）。
    pub thumbnail: String,
    /// 来源："builtin" | "user"，市场页据此决定能否删除。
    pub source: String,
}
```

- [ ] 5. `pet_skin.rs`：用下面三个函数**整体替换**现有 `skin_dir`（`valid_skin_id` 保留不动）：

```rust
/// 解析皮肤来源：用户目录优先于内置目录；都不存在返回 None。
pub fn skin_source(id: &str) -> Option<&'static str> {
    if !valid_skin_id(id) {
        return None;
    }
    if let Some(user_root) = user_skins_dir() {
        if user_root.join(id).join("skin.json").is_file() {
            return Some(SKIN_SOURCE_USER);
        }
    }
    let builtin_root = skins_dir()?;
    if builtin_root.join(id).join("skin.json").is_file() {
        return Some(SKIN_SOURCE_BUILTIN);
    }
    None
}

/// 皮肤目录（id 已校验）：用户目录优先，其次内置目录。
pub fn skin_dir(id: &str) -> Option<PathBuf> {
    if !valid_skin_id(id) {
        return None;
    }
    if skin_source(id) == Some(SKIN_SOURCE_USER) {
        return user_skins_dir().map(|root| root.join(id));
    }
    skins_dir().map(|root| root.join(id))
}

/// 皮肤内文件 → 前端可直显 URL：
/// 内置皮肤返回相对 `ui/` 的路径（webview 根即 ui/）；
/// 用户皮肤返回 asset protocol URL（需 tauri.conf.json 开启 assetProtocol）。
pub fn skin_file_url(source: &str, skin_id: &str, file_name: &str) -> String {
    if source == SKIN_SOURCE_USER {
        if let Some(path) = user_skins_dir().map(|root| root.join(skin_id).join(file_name)) {
            return asset_url(&path);
        }
    }
    format!("assets/skins/{skin_id}/{file_name}")
}

/// Windows asset protocol URL：`http://asset.localhost/<encodeURIComponent(path)>`。
/// 逐字节百分号编码 UTF-8，保留字符与前端 `convertFileSrc` 完全一致
/// （字母数字与 `-_.~!*'()` 保留，其余—including `/` 与 `\`—编码为 %XX）。
fn asset_url(path: &Path) -> String {
    let raw = path.to_string_lossy();
    let mut out = String::from("http://asset.localhost/");
    for byte in raw.as_bytes() {
        let keep = matches!(
            byte,
            b'A'..=b'Z' | b'a'..=b'z' | b'0'..=b'9'
                | b'-' | b'_' | b'.' | b'~' | b'!' | b'*' | b'\'' | b'(' | b')'
        );
        if keep {
            out.push(*byte as char);
        } else {
            out.push_str(&format!("%{byte:02X}"));
        }
    }
    out
}
```

- [ ] 6. `pet_skin.rs`：把现有 `load_manifest` **整体替换**为「参数化 + 委托」两个函数（校验主体与现状逐行一致，仅目录来源改为参数）：

```rust
/// 解析并校验指定目录中的皮肤清单。目录必须存在且每个状态四选一：
/// 静态图或完整 spritesheet 动画配置；两者都缺 / 都有视为非法。
/// 市场安装链在把临时目录落地为正式目录前，也用本函数预校验。
pub fn load_manifest_at(dir: &Path, id: &str) -> Result<PetSkinManifest, String> {
    let manifest_path = dir.join("skin.json");
    let raw = std::fs::read_to_string(&manifest_path)
        .map_err(|error| format!("读取皮肤清单失败（{}）：{error}", manifest_path.display()))?;
    let manifest: PetSkinManifest =
        serde_json::from_str(&raw).map_err(|error| format!("皮肤清单格式错误（{id}）：{error}"))?;
    if manifest.id != id {
        return Err(format!(
            "皮肤目录与清单 id 不一致：目录 {id}，清单 {}",
            manifest.id
        ));
    }
    if manifest.name.trim().is_empty() {
        return Err(format!("皮肤 {id} 缺少 name"));
    }
    for state in PET_STATES {
        let entry = manifest
            .states
            .get(state)
            .ok_or_else(|| format!("皮肤 {id} 缺少 {state} 状态"))?;
        let has_image = entry.image.as_deref().is_some_and(|name| !name.is_empty());
        let sheet = entry.sheet.as_deref().filter(|name| !name.is_empty());
        if has_image == sheet.is_some() {
            return Err(format!(
                "皮肤 {id} 的 {state} 必须且只能指定 image 或 sheet 之一"
            ));
        }
        if let Some(name) = sheet {
            if !valid_image_name(name) {
                return Err(format!("皮肤 {id} 的 {state} sheet 名非法：{name}"));
            }
            if !dir.join(name).is_file() {
                return Err(format!("皮肤 {id} 的 {state} spritesheet 缺失：{name}"));
            }
            let frame = manifest.frame.as_ref().ok_or_else(|| {
                format!("皮肤 {id} 使用 spritesheet 但缺少 frame 帧尺寸声明")
            })?;
            if frame.width == 0 || frame.height == 0 {
                return Err(format!("皮肤 {id} 的 frame 尺寸必须为正数"));
            }
            if entry.row.is_none() || entry.frames.is_none() {
                return Err(format!("皮肤 {id} 的 {state} 缺少 row 或 frames"));
            }
        } else if let Some(name) = entry.image.as_deref() {
            if !valid_image_name(name) {
                return Err(format!("皮肤 {id} 的 {state} 图片名非法：{name}"));
            }
            if !dir.join(name).is_file() {
                return Err(format!("皮肤 {id} 的 {state} 图片缺失：{name}"));
            }
        }
    }
    Ok(manifest)
}

/// 按双源查找并校验皮肤清单。
pub fn load_manifest(id: &str) -> Result<PetSkinManifest, String> {
    let dir = skin_dir(id).ok_or_else(|| format!("皮肤目录不可用：{id}"))?;
    load_manifest_at(&dir, id)
}
```

- [ ] 7. `pet_skin.rs`：`list_skins` **整体替换**为双根扫描版（用户源在前、按 id 去重，临时目录 `.tmp-*` / `.old-*` 因含 `.` 被 `valid_skin_id` 自动跳过）：

```rust
/// 列出全部有效皮肤（内置 + 用户，用户源优先；无效目录跳过并记日志）。
pub fn list_skins() -> Vec<PetSkinInfo> {
    let mut skins = Vec::new();
    let mut seen: Vec<String> = Vec::new();
    let roots = [
        user_skins_dir().map(|root| (root, SKIN_SOURCE_USER)),
        skins_dir().map(|root| (root, SKIN_SOURCE_BUILTIN)),
    ];
    for (root, source) in roots.into_iter().flatten() {
        let Ok(entries) = std::fs::read_dir(&root) else {
            continue;
        };
        for entry in entries.flatten() {
            let Some(id) = entry.file_name().to_str().map(str::to_string) else {
                continue;
            };
            if !valid_skin_id(&id) || !entry.path().is_dir() || seen.contains(&id) {
                continue;
            }
            let manifest = match load_manifest(&id) {
                Ok(manifest) => manifest,
                Err(error) => {
                    eprintln!("[lingxi] pet_skin: 跳过无效皮肤 {id}：{error}");
                    continue;
                }
            };
            seen.push(id.clone());
            // 缩略图：静态图取 idle 图片；spritesheet 取 idle 行第一帧
            //（`<url>#<row>`，前端按帧尺寸裁剪显示）。
            let thumbnail = manifest
                .states
                .get("idle")
                .map(|state| {
                    if let Some(image) = state.image.as_deref().filter(|n| !n.is_empty()) {
                        skin_file_url(source, &id, image)
                    } else {
                        let row = state.row.unwrap_or(0);
                        let sheet = state.sheet.clone().unwrap_or_default();
                        format!("{}#{row}", skin_file_url(source, &id, &sheet))
                    }
                })
                .unwrap_or_default();
            skins.push(PetSkinInfo {
                id: manifest.id,
                name: manifest.name,
                author: manifest.author,
                version: manifest.version,
                description: manifest.description,
                thumbnail,
                source: source.to_string(),
            });
        }
    }
    skins.sort_by(|a, b| a.id.cmp(&b.id));
    skins
}
```

- [ ] 8. `pet_skin.rs`：`view_for` 内做三处小改。在 `let manifest = load_manifest(skin_id)…` 语句结束后新增一行取来源，并把两处图片 URL 换成 `skin_file_url`：

```rust
    // ↓ 新增（位于 Ok(manifest) 拿到之后、构建 images 之前）
    let source = skin_source(&manifest.id).unwrap_or(SKIN_SOURCE_BUILTIN);
```

```rust
            images.insert(state.clone(), skin_file_url(source, &manifest.id, name));
```

```rust
                    sheet: skin_file_url(source, &manifest.id, sheet),
```

- [ ] 9. 验证（在 `apps/overlay` 目录执行）：

```bash
cargo test
cargo build
```

预期：编译通过、既有测试仍绿。启动应用后设置页皮肤卡片正常显示（现状皮肤 `source` 应为 `builtin`，行为与改造前一致）。用户皮肤图片通路在 Task 7 端到端验收中检验。

---

## Task 3：market_list 命令（清单合并 + 安装状态标注）

**Files**：`apps/overlay/src/settings.rs`、`apps/overlay/src/market.rs`、`apps/overlay/src/main.rs`

- [ ] 1. `settings.rs`：`BackendSettings` 结构体加字段（放在 `panel_remember_position` 之后），`Default` 实现同步加默认值：

```rust
    /// 远程市场清单地址；空 = 仅使用内置清单（离线兜底）。
    pub(crate) market_url: String,
```

```rust
            market_url: String::new(),
```

- [ ] 2. `market.rs`：头部 use 区替换为（新增 `std::io::Read`、`tauri::State`、state 模块引用）：

```rust
use serde::{Deserialize, Serialize};
use std::io::Read as _;
use std::path::PathBuf;

use tauri::State;

use crate::pet_skin;
use crate::state::{AppState, MutexExt};
```

- [ ] 3. `market.rs`：在 `sanitize_entry` 之后、`#[cfg(test)]` 之前新增清单获取与合并（约 60 行）：

```rust
/// 拉取远程清单文本：30 秒超时、最多 3 次重定向、清单体积上限 1 MB。
fn fetch_catalog_text(url: &str) -> Result<String, String> {
    let agent = ureq::AgentBuilder::new()
        .timeout(std::time::Duration::from_secs(30))
        .redirects(3)
        .build();
    let response = agent
        .get(url)
        .call()
        .map_err(|error| format!("拉取市场清单失败：{error}"))?;
    let mut text = String::new();
    response
        .into_reader()
        .take(1024 * 1024)
        .read_to_string(&mut text)
        .map_err(|error| format!("读取市场清单失败：{error}"))?;
    Ok(text)
}

/// 解析清单文本：JSON 结构错误返回 Err；单条字段非法则丢弃该条（记日志）。
pub fn parse_catalog(text: &str) -> Result<MarketCatalog, String> {
    let catalog: MarketCatalog =
        serde_json::from_str(text).map_err(|error| format!("市场清单格式错误：{error}"))?;
    let entries = catalog
        .entries
        .into_iter()
        .filter_map(sanitize_entry)
        .collect();
    Ok(MarketCatalog { entries })
}

/// 合并清单：内置为兜底；设置了 `market_url` 且拉取成功时，远程条目按 id 优先覆盖。
/// 远程任何环节失败 → 静默降级为仅内置清单（记日志，不向用户报错）。
pub fn merged_entries(state: &AppState) -> Vec<MarketEntry> {
    let mut entries: Vec<MarketEntry> = Vec::new();
    if let Some(path) = builtin_catalog_path() {
        match std::fs::read_to_string(&path) {
            Ok(text) => match parse_catalog(&text) {
                Ok(catalog) => entries.extend(catalog.entries),
                Err(error) => eprintln!("[lingxi] market: 内置清单解析失败：{error}"),
            },
            Err(error) => eprintln!("[lingxi] market: 读取内置清单失败：{error}"),
        }
    }
    let remote_url = {
        let settings = state.backend.safe_lock();
        settings.market_url.trim().to_string()
    };
    if remote_url.is_empty() {
        return entries;
    }
    match fetch_catalog_text(&remote_url).and_then(|text| parse_catalog(&text)) {
        Ok(remote) => {
            for entry in remote.entries {
                match entries.iter_mut().find(|existing| existing.id == entry.id) {
                    Some(existing) => *existing = entry,
                    None => entries.push(entry),
                }
            }
        }
        Err(error) => eprintln!("[lingxi] market: 远程清单不可用，降级内置清单：{error}"),
    }
    entries
}

/// 查询皮肤本机安装状态：Some((来源, 版本))。清单校验失败视为未安装。
fn installed_info(id: &str) -> Option<(String, String)> {
    let source = pet_skin::skin_source(id)?;
    let version = pet_skin::load_manifest(id).ok()?.version;
    Some((source.to_string(), version))
}

/// 市场页数据：合并清单 + 本机安装状态标注。
#[tauri::command]
pub(crate) fn market_list(state: State<AppState>) -> Result<Vec<MarketItem>, String> {
    let entries = merged_entries(state.inner());
    let mut items = Vec::with_capacity(entries.len());
    for entry in entries {
        let (installed_source, installed_version) =
            installed_info(&entry.id).unwrap_or_default();
        let updatable =
            !installed_version.is_empty() && version_newer(&entry.version, &installed_version);
        items.push(MarketItem {
            id: entry.id,
            name: entry.name,
            author: entry.author,
            version: entry.version,
            description: entry.description,
            size: entry.size,
            installed_source,
            installed_version,
            updatable,
        });
    }
    Ok(items)
}
```

- [ ] 4. `main.rs`：`invoke_handler` 的 `generate_handler![…]` 列表内、桌宠命令（`pet::*` 系列）之后追加一行：

```rust
            market::market_list,
```

- [ ] 5. 验证（在 `apps/overlay` 目录执行）：

```bash
cargo test
cargo build
```

预期：编译通过。启动应用后可临时在设置文件 `%APPDATA%/lingxi/settings.json` 手工加 `"market_url": ""`（或省略）验证旧配置兼容；命令联通性在 Task 7 前端接好后整体验收。

---

## Task 4：market_install 命令（下载 → 校验 → 解压 → 落地 → 回滚）

**Files**：`apps/overlay/src/market.rs`、`apps/overlay/src/main.rs`

安装链固定顺序（对应第二章 2.2/2.3）：临时目录 → 下载+sha256 → 安全解压（含 zip 顶层目录兼容）→ 清单预校验 → 同卷 rename 落地（升级先备份 `.old`）→ 任一步失败全量清理。

- [ ] 1. `market.rs`：头部 use 区替换为最终形态（新增 sha2、Write、Path、SystemTime）：

```rust
use serde::{Deserialize, Serialize};
use sha2::{Digest, Sha256};
use std::io::{Read as _, Write as _};
use std::path::{Path, PathBuf};
use std::time::{SystemTime, UNIX_EPOCH};

use tauri::State;

use crate::pet_skin;
use crate::state::{AppState, MutexExt};
```

- [ ] 2. `market.rs`：在 `market_list` 之后新增四个安装链函数与命令：

```rust
/// 当前毫秒时间戳（临时目录名去重用）。
fn now_millis() -> u128 {
    SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .map(|duration| duration.as_millis())
        .unwrap_or(0)
}

/// 下载 url 到 dest_path，边下边算 sha256；返回十六进制摘要。
/// 体积与 MAX_UNCOMPRESSED 共用限额，超限即中止。
fn download_to(url: &str, dest_path: &Path) -> Result<String, String> {
    let agent = ureq::AgentBuilder::new()
        .timeout(std::time::Duration::from_secs(30))
        .redirects(3)
        .build();
    let response = agent
        .get(url)
        .call()
        .map_err(|error| format!("下载失败：{error}"))?;
    let mut reader = response.into_reader();
    let file = std::fs::File::create(dest_path)
        .map_err(|error| format!("创建下载文件失败：{error}"))?;
    let mut writer = std::io::BufWriter::new(file);
    let mut hasher = Sha256::new();
    let mut buffer = [0u8; 64 * 1024];
    let mut downloaded: u64 = 0;
    loop {
        let read = reader
            .read(&mut buffer)
            .map_err(|error| format!("下载中断：{error}"))?;
        if read == 0 {
            break;
        }
        downloaded += read as u64;
        if downloaded > MAX_UNCOMPRESSED {
            return Err("下载内容超过大小限额".into());
        }
        hasher.update(&buffer[..read]);
        writer
            .write_all(&buffer[..read])
            .map_err(|error| format!("写入下载文件失败：{error}"))?;
    }
    writer
        .flush()
        .map_err(|error| format!("写入下载文件失败：{error}"))?;
    Ok(format!("{:x}", hasher.finalize()))
}

/// 安全解压 zip 到目标目录：`enclosed_name` 防 zip-slip、扩展名黑名单、
/// 文件数与累计体积限额。zip 路径穿越 / 夹带可执行文件时直接报错。
fn safe_extract(zip_path: &Path, dest_dir: &Path) -> Result<(), String> {
    let file =
        std::fs::File::open(zip_path).map_err(|error| format!("打开下载文件失败：{error}"))?;
    let mut archive = zip::ZipArchive::new(std::io::BufReader::new(file))
        .map_err(|error| format!("压缩包读取失败：{error}"))?;
    if archive.len() > MAX_FILES {
        return Err("压缩包文件数超过限额".into());
    }
    let mut total: u64 = 0;
    for index in 0..archive.len() {
        let mut entry = archive
            .by_index(index)
            .map_err(|error| format!("压缩包读取失败：{error}"))?;
        let Some(relative) = entry.enclosed_name() else {
            return Err("压缩包内含非法路径（疑似路径穿越）".into());
        };
        if let Some(ext) = relative
            .extension()
            .and_then(|value| value.to_str())
            .map(|value| value.to_ascii_lowercase())
        {
            if BLOCKED_EXTS.contains(&ext.as_str()) {
                return Err(format!("压缩包内含禁止的文件类型：{ext}"));
            }
        }
        let out_path = dest_dir.join(relative);
        if entry.is_dir() {
            std::fs::create_dir_all(&out_path).map_err(|error| format!("创建目录失败：{error}"))?;
            continue;
        }
        if let Some(parent) = out_path.parent() {
            std::fs::create_dir_all(parent).map_err(|error| format!("创建目录失败：{error}"))?;
        }
        total += entry.size();
        if total > MAX_UNCOMPRESSED {
            return Err("解压内容超过大小限额".into());
        }
        let mut out_file = std::fs::File::create(&out_path)
            .map_err(|error| format!("写入解压文件失败：{error}"))?;
        std::io::copy(&mut entry, &mut out_file)
            .map_err(|error| format!("写入解压文件失败：{error}"))?;
    }
    Ok(())
}

/// 兼容 zip 带顶层目录的情况：根部没有 skin.json 但唯一子目录里有，
/// 则把该子目录内容上移到根部（皮肤包标准结构 = 根部即皮肤目录）。
fn flatten_single_root(dir: &Path) -> Result<(), String> {
    if dir.join("skin.json").is_file() {
        return Ok(());
    }
    let entries: Vec<_> = std::fs::read_dir(dir)
        .map_err(|error| format!("读取临时目录失败：{error}"))?
        .collect::<Result<_, _>>()
        .map_err(|error| format!("读取临时目录失败：{error}"))?;
    if entries.len() != 1 || !entries[0].path().is_dir() {
        return Err("压缩包结构不符合要求（根部缺少 skin.json）".into());
    }
    let inner = entries[0].path();
    for entry in std::fs::read_dir(&inner)
        .map_err(|error| format!("读取压缩包子目录失败：{error}"))?
        .flatten()
    {
        let target = dir.join(entry.file_name());
        std::fs::rename(entry.path(), &target)
            .map_err(|error| format!("整理压缩包结构失败：{error}"))?;
    }
    std::fs::remove_dir(&inner).map_err(|error| format!("整理压缩包结构失败：{error}"))?;
    Ok(())
}

/// 下载并安装市场皮肤到用户目录（含升级覆盖与失败回滚）。
#[tauri::command]
pub(crate) fn market_install(
    app: tauri::AppHandle,
    state: State<AppState>,
    id: String,
) -> Result<(), String> {
    if !pet_skin::valid_skin_id(&id) {
        return Err("皮肤 id 非法".into());
    }
    let entry = merged_entries(state.inner())
        .into_iter()
        .find(|candidate| candidate.id == id)
        .ok_or_else(|| format!("市场清单中找不到皮肤：{id}"))?;
    if pet_skin::skin_source(&id) == Some(pet_skin::SKIN_SOURCE_BUILTIN) {
        return Err("与内置皮肤冲突，无法安装".into());
    }
    let user_root =
        pet_skin::user_skins_dir().ok_or_else(|| "无法定位用户皮肤目录".to_string())?;
    std::fs::create_dir_all(&user_root).map_err(|error| format!("创建用户皮肤目录失败：{error}"))?;

    let tmp_dir = user_root.join(format!(".tmp-{id}-{}", now_millis()));
    if tmp_dir.exists() {
        let _ = std::fs::remove_dir_all(&tmp_dir);
    }
    std::fs::create_dir_all(&tmp_dir).map_err(|error| format!("创建临时目录失败：{error}"))?;
    let cleanup = |dir: &Path| {
        let _ = std::fs::remove_dir_all(dir);
    };

    // 1) 下载 + sha256 校验
    let zip_path = tmp_dir.join("download.zip");
    let actual = match download_to(&entry.url, &zip_path) {
        Ok(actual) => actual,
        Err(error) => {
            cleanup(&tmp_dir);
            return Err(error);
        }
    };
    if actual != entry.sha256.to_ascii_lowercase() {
        cleanup(&tmp_dir);
        return Err("文件校验失败，已取消安装".into());
    }

    // 2) 安全解压 + 结构归一 + 清单预校验
    if let Err(error) = safe_extract(&zip_path, &tmp_dir) {
        cleanup(&tmp_dir);
        return Err(error);
    }
    let _ = std::fs::remove_file(&zip_path);
    if let Err(error) = flatten_single_root(&tmp_dir) {
        cleanup(&tmp_dir);
        return Err(error);
    }
    if let Err(error) = pet_skin::load_manifest_at(&tmp_dir, &id) {
        cleanup(&tmp_dir);
        return Err(format!("皮肤包校验失败：{error}"));
    }

    // 3) 落地：升级场景先备份旧目录到 .old，rename 成功后删除备份；
    //    rename 失败则把备份还原，保证旧版本继续可用。
    let final_dir = user_root.join(&id);
    let old_dir = user_root.join(format!(".old-{id}"));
    let had_old = final_dir.exists();
    if had_old {
        let _ = std::fs::remove_dir_all(&old_dir);
        if let Err(error) = std::fs::rename(&final_dir, &old_dir) {
            cleanup(&tmp_dir);
            return Err(format!("备份旧版本失败：{error}"));
        }
    }
    if let Err(error) = std::fs::rename(&tmp_dir, &final_dir) {
        if had_old {
            let _ = std::fs::rename(&old_dir, &final_dir);
        }
        cleanup(&tmp_dir);
        return Err(format!("安装失败：{error}"));
    }
    if had_old {
        let _ = std::fs::remove_dir_all(&old_dir);
    }

    // 4) 升级的正是使用中皮肤 → 重载并广播，桌宠立即换新版本
    let (is_current, overrides, visible) = {
        let settings = state.backend.safe_lock();
        (
            settings.pet_skin == id,
            settings.pet_bubble_overrides.clone(),
            settings.pet_visible,
        )
    };
    if is_current {
        if let Ok(view) = pet_skin::view_for(&id, &overrides, visible) {
            let _ = app.emit("pet-config-changed", &view);
        }
    }
    Ok(())
}
```

- [ ] 3. `main.rs`：`generate_handler![…]` 列表内 `market::market_list,` 之后追加：

```rust
            market::market_install,
```

- [ ] 4. 验证（在 `apps/overlay` 目录执行）：

```bash
cargo test
cargo build
```

预期：编译通过、既有测试仍绿。安装链手工联调（配本地演示源）在 Task 7 统一进行；本 Task 先保证 `sha256` 大小写不敏感比较、临时目录 `.tmp-*` 命名不影响 `list_pet_skins`。

---

## Task 5：`market_uninstall` —— 删除用户皮肤（闭环的「删」）

**改动文件**：`apps/overlay/src/market.rs`、`apps/overlay/src/main.rs`

**行为定义**（落实第三章 3.1 / 3.2）：

| 场景 | 行为 |
| --- | --- |
| id 非法 / 皮肤不存在 | 返回错误，不做任何改动 |
| 内置皮肤 | 报「内置皮肤不可删除」 |
| 用户皮肤、非使用中 | 直接删除目录，不广播（桌宠视图无变化） |
| 用户皮肤、使用中 | 先回落 `lingxi-hamster` 并广播；回落成功才删目录，回落失败则中止删除 |

- [ ] 1. `market.rs` 文件末尾追加卸载命令与回落辅助函数：

```rust
/// 卸载用户皮肤。内置皮肤不可删除；删除使用中的皮肤时先回落默认皮肤，
/// 回落失败则中止删除，保证桌宠永远停留在可用皮肤上。
/// #[tauri::command(async)]：与另两个市场命令保持一致，磁盘 IO 也离开主线程。
#[tauri::command(async)]
pub(crate) fn market_uninstall(
    app: tauri::AppHandle,
    state: State<AppState>,
    id: String,
) -> Result<(), String> {
    if !pet_skin::valid_skin_id(&id) {
        return Err("皮肤 id 非法".into());
    }
    match pet_skin::skin_source(&id) {
        Some(pet_skin::SKIN_SOURCE_USER) => {}
        Some(_) => return Err("内置皮肤不可删除".into()),
        None => return Err("皮肤不存在".into()),
    }
    let user_root =
        pet_skin::user_skins_dir().ok_or_else(|| "无法定位用户皮肤目录".to_string())?;
    let target = user_root.join(&id);
    if !target.starts_with(&user_root) {
        return Err("非法的皮肤路径".into());
    }

    // 使用中 → 先回落；只有回落成功才允许删目录，否则桌宠会指向已丢失的皮肤。
    let is_current = state.backend.safe_lock().pet_skin == id;
    if is_current {
        switch_to_default_skin(&app, &state)?;
    }

    std::fs::remove_dir_all(&target).map_err(|error| format!("删除皮肤目录失败：{error}"))?;
    Ok(())
}

/// 回落默认皮肤：完整复刻 `pet.rs::set_pet_skin` 的既有模式
/// （校验默认皮肤可加载 → 锁内改配置并持久化 → 解锁重建视图 → 广播）。
/// 与 market_install 末尾"尽力广播"不同，这里用 `?` 上抛：回落失败必须中止删除。
fn switch_to_default_skin(
    app: &tauri::AppHandle,
    state: &State<AppState>,
) -> Result<(), String> {
    let default_id = pet_skin::DEFAULT_SKIN_ID;
    // 先确认默认皮肤清单可加载（内置皮肤正常必然成功，双保险）。
    pet_skin::load_manifest(default_id)?;
    let (overrides, visible) = {
        let mut backend = state.backend.safe_lock();
        backend.pet_skin = default_id.to_string();
        crate::settings::persist_backend_settings(&backend);
        (backend.pet_bubble_overrides.clone(), backend.pet_visible)
    };
    let view = pet_skin::view_for(default_id, &overrides, visible)?;
    let _ = app.emit("pet-config-changed", &view);
    Ok(())
}
```

- [ ] 2. `main.rs`：`generate_handler![…]` 列表内 `market::market_install,` 之后追加：

```rust
            market::market_uninstall,
```

- [ ] 3. 验证（在 `apps/overlay` 目录执行）：

```bash
cargo test
cargo build
```

预期：编译通过、既有测试仍绿。行为核对点：①对 `lingxi-hamster` 调卸载必须报「内置皮肤不可删除」；②删除非使用中的用户皮肤后 `list_pet_skins` 不再返回它；③删除使用中的皮肤时，`settings.json` 的 `pet_skin` 已先被改写为 `lingxi-hamster`，且桌宠窗口收到 `pet-config-changed`。UI 联动（删除后刷新市场页）在 Task 6 接线。

---

## Task 6（上）：市场页 UI —— index.html 结构 + styles.css 样式

**改动文件**：`apps/overlay/ui/index.html`、`apps/overlay/ui/styles.css`

**交互结构**（对应第四章六步主流程）：工具页横幅从「即将开放（disabled）」变为可点击入口；市场视图 = 顶栏（返回 / 标题 / 刷新）+ 两页签（市场 / 已安装）+ 错误条 + 卡片网格 + 空态。全部样式复用既有 Fluent 暗色变量（`--surface`/`--accent`/`--del-bg` 等），不引入新设计语言。

- [ ] 1. `index.html` L141 横幅按钮：去掉 `disabled`，aria-label 同步改语义：

```html
<!-- 改前 -->
        <button class="market-banner" id="tools-market-btn" type="button" disabled aria-label="插件市场，即将开放">
<!-- 改后 -->
        <button class="market-banner" id="tools-market-btn" type="button" aria-label="打开插件市场">
```

- [ ] 2. `index.html` L147 徽章文案同步开放语义：

```html
<!-- 改前 -->
          <span class="market-banner-badge">即将开放</span>
<!-- 改后 -->
          <span class="market-banner-badge">进入</span>
```

- [ ] 3. `index.html`：定位工具页区块结尾（紧跟着一行 `<div class="tools-empty" id="tools-empty" hidden>未找到匹配的工具</div>` 的 `</section>`），在其后插入市场视图：

```html
      <section class="market-view" id="market-view" hidden>
        <div class="market-toolbar">
          <button class="market-back" id="market-back" type="button">
            <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
            返回
          </button>
          <span class="market-title">插件市场</span>
          <button class="market-refresh" id="market-refresh" type="button">刷新</button>
        </div>
        <div class="market-tabs" id="market-tabs">
          <button class="market-tab is-active" data-tab="market">市场</button>
          <button class="market-tab" data-tab="installed">已安装</button>
        </div>
        <div class="market-error" id="market-error" hidden></div>
        <div class="market-grid" id="market-grid"></div>
        <div class="market-empty" id="market-empty" hidden>暂无可安装的皮肤</div>
        <div class="market-empty" id="installed-empty" hidden>还没有安装任何皮肤</div>
      </section>
```

- [ ] 4. `styles.css` 文件末尾追加市场页样式（横幅 L998-1074 既有样式不动，以下全部为新增）：

```css
/* ---- 插件市场页 ---- */
.market-view {
  flex: 1 1 auto;
  min-height: 0;
  overflow-y: auto;
  padding: 14px 16px 18px;
}
.market-toolbar {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 12px;
}
.market-back {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  padding: 5px 10px 5px 7px;
  font-size: var(--fs-sm);
  font-weight: 600;
  color: var(--text-dim);
  background: transparent;
  border: 1px solid var(--stroke);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: color 0.15s, border-color 0.15s;
}
.market-back:hover { color: var(--text); border-color: var(--stroke-strong); }
.market-title { flex: 1 1 auto; font-size: var(--fs-lg); font-weight: 700; }
.market-refresh {
  padding: 5px 12px;
  font-size: var(--fs-sm);
  font-weight: 600;
  color: var(--text-dim);
  background: transparent;
  border: 1px solid var(--stroke);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: color 0.15s, border-color 0.15s;
}
.market-refresh:hover:not(:disabled) { color: var(--text); border-color: var(--stroke-strong); }
.market-refresh:disabled { opacity: 0.55; cursor: default; }
.market-tabs {
  display: flex;
  gap: 4px;
  margin-bottom: 12px;
  padding: 3px;
  background: var(--surface);
  border: 1px solid var(--stroke);
  border-radius: var(--radius-sm);
}
.market-tab {
  flex: 1 1 0;
  padding: 6px 0;
  font-size: var(--fs-sm);
  font-weight: 600;
  color: var(--text-dim);
  background: transparent;
  border: 0;
  border-radius: 4px;
  cursor: pointer;
  transition: color 0.15s, background 0.15s;
}
.market-tab.is-active { color: var(--on-accent); background: var(--accent); }
.market-error {
  margin-bottom: 10px;
  padding: 8px 12px;
  font-size: var(--fs-sm);
  color: var(--del-fg);
  background: var(--del-bg);
  border: 1px solid rgba(255, 153, 164, 0.3);
  border-radius: var(--radius-sm);
}
.market-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 10px;
}
.market-card {
  display: flex;
  flex-direction: column;
  gap: 7px;
  padding: 10px;
  background: var(--surface);
  border: 1px solid var(--stroke);
  border-radius: var(--radius-md);
}
.market-card-thumb {
  width: 100%;
  aspect-ratio: 1;
  border-radius: var(--radius-sm);
  background-color: var(--accent-dim);
  background-size: cover;
  background-position: center;
  image-rendering: pixelated;
  overflow: hidden;
}
.market-card-name { font-size: var(--fs-md); font-weight: 600; }
.market-card-meta { font-size: var(--fs-xs); color: var(--text-faint); }
.market-card-desc {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  min-height: 2.6em;
  font-size: var(--fs-xs);
  color: var(--text-dim);
}
.market-card-foot {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  margin-top: auto;
}
.market-state-badge {
  padding: 2px 8px;
  font-size: var(--fs-xs);
  font-weight: 600;
  white-space: nowrap;
  color: var(--text-faint);
  background: var(--surface-hi);
  border-radius: 999px;
}
.market-state-badge.updatable { color: var(--warn); background: rgba(255, 212, 94, 0.12); }
.market-state-badge.current { color: var(--add-fg); background: var(--add-bg); }
.market-action {
  padding: 5px 12px;
  font-size: var(--fs-sm);
  font-weight: 700;
  white-space: nowrap;
  color: var(--on-accent);
  background: var(--accent);
  border: 0;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background 0.15s, opacity 0.15s;
}
.market-action:hover:not(:disabled) { background: var(--accent-hi); }
.market-action:disabled { cursor: default; opacity: 0.55; }
.market-action.danger { color: var(--del-fg); background: var(--del-bg); }
.market-action.danger:hover:not(:disabled) { background: rgba(255, 153, 164, 0.22); }
.market-empty {
  padding: 28px 0;
  text-align: center;
  font-size: var(--fs-sm);
  color: var(--text-faint);
}
```

- [ ] 5. 验证：`cd apps/overlay && cargo build` 后启动应用（或直接刷新 webview），确认——①横幅可点击、徽章显示「进入」；②市场视图默认 `hidden` 不影响工具页布局；③市场页空数据时只有顶栏 + 页签 + 空态文案，无样式塌陷。

---

## Task 6（下）：市场页逻辑 —— market.rs 补缩略图字段 + app.js 渲染与按钮状态机

**改动文件**：`apps/overlay/src/market.rs`、`apps/overlay/ui/app.js`

**先做一致性回改**：市场卡片需要预览图，Task 1/Task 3 的结构当时未含 `thumbnail`，本步一并补齐（同步修订了第 5.2 节字段表与 4.3 命令表）。

- [ ] 1. `market.rs` 四处回改——①`MarketEntry` 末尾（`size` 字段后）加：

```rust
    /// 卡片预览图直链（可选）。缺省或非法时前端用色块占位。
    #[serde(default)]
    pub thumbnail: String,
```

②`MarketItem` 的 `description` 字段后加 `pub thumbnail: String,`；③`sanitize_entry` 的 sha256 校验之后、`Some(entry)` 之前改为（非法缩略图只清空、不丢条目）：

```rust
    // 缩略图非法只清空、不丢条目：预览是加分项，不影响安装能力。
    let mut entry = entry;
    if !entry.thumbnail.is_empty() && !valid_url(&entry.thumbnail) {
        entry.thumbnail.clear();
    }
    Some(entry)
```

④`demo_entry()` 补 `thumbnail: String::new(),`，`market_list` 的 `items.push(MarketItem {…})` 中 `size: entry.size,` 之后补 `thumbnail: entry.thumbnail,`。

- [ ] 2. `app.js`：`el` 映射表 `widgetsGrid` 行后、闭括号 `};` 前追加：

```js
  // Market
  marketView: document.getElementById("market-view"),
  marketGrid: document.getElementById("market-grid"),
  marketEmpty: document.getElementById("market-empty"),
  installedEmpty: document.getElementById("installed-empty"),
  marketError: document.getElementById("market-error"),
  marketBack: document.getElementById("market-back"),
  marketRefresh: document.getElementById("market-refresh"),
  marketTabs: document.getElementById("market-tabs"),
  marketBanner: document.getElementById("tools-market-btn"),
```

- [ ] 3. `app.js`：`let toolsActiveCat = "all";` 行后追加状态变量：

```js
let marketCache = [];
let installedCache = [];
let currentSkinId = "";
let marketTab = "market";
let marketBusyId = "";
```

- [ ] 4. `app.js` `showView` 三处小改——①`el.toolsView.hidden = view !== "tools";` 行后加 `el.marketView.hidden = view !== "market";`；②`map` 对象加 `market: el.toolsTab`（市场归属工具页签高亮）；③`if (view === "tools") …` 行后加 `if (view === "market") loadMarket();`。

- [ ] 5. `app.js`：定位 `function fillPetSettings(view) {`，在其上方插入整块市场逻辑：

```js
// ---- 插件市场 ----

async function loadMarket() {
  if (!invoke) return;
  el.marketRefresh.disabled = true;
  try {
    const [items, skins, config] = await Promise.all([
      invoke("market_list"),
      invoke("list_pet_skins"),
      invoke("current_pet_config"),
    ]);
    marketCache = items;
    installedCache = skins;
    currentSkinId = config.skin.id;
    el.marketError.hidden = true;
  } catch (e) {
    el.marketError.textContent = "市场加载失败：" + e;
    el.marketError.hidden = false;
  } finally {
    el.marketRefresh.disabled = false;
  }
  renderMarketView();
}

function renderMarketView() {
  el.marketGrid.textContent = "";
  if (marketTab === "market") {
    el.marketEmpty.hidden = marketCache.length > 0;
    el.installedEmpty.hidden = true;
    for (const item of marketCache) el.marketGrid.appendChild(renderMarketCard(item));
  } else {
    el.marketEmpty.hidden = true;
    el.installedEmpty.hidden = installedCache.length > 0;
    for (const skin of installedCache) el.marketGrid.appendChild(renderInstalledCard(skin));
  }
}

// 缩略图兼容两种格式：纯图片 URL，或「URL#row,cols,rows」雪碧图帧
//（与 renderSkinGrid 的解析规则一致）。
function applyThumbnail(thumb, thumbnail) {
  if (!thumbnail) return;
  const hash = thumbnail.indexOf("#");
  if (hash === -1) {
    thumb.style.backgroundImage = `url("${thumbnail}")`;
    return;
  }
  const meta = thumbnail.slice(hash + 1).split(",").map((n) => parseInt(n, 10) || 0);
  const row = meta[0] || 0;
  const cols = meta[1] || 8;
  const rows = meta[2] || 9;
  thumb.style.backgroundImage = `url("${thumbnail.slice(0, hash)}")`;
  thumb.style.backgroundSize = `${cols * 100}% ${rows * 100}%`;
  thumb.style.backgroundPosition = `0% ${(rows > 1 ? (row / (rows - 1)) * 100 : 0).toFixed(2)}%`;
}

function renderMarketCard(item) {
  const card = document.createElement("div");
  card.className = "market-card";
  const thumb = document.createElement("div");
  thumb.className = "market-card-thumb";
  applyThumbnail(thumb, item.thumbnail);
  const name = document.createElement("span");
  name.className = "market-card-name";
  name.textContent = item.name;
  const meta = document.createElement("small");
  meta.className = "market-card-meta";
  meta.textContent = [item.author, item.version].filter(Boolean).join(" · ");
  const desc = document.createElement("p");
  desc.className = "market-card-desc";
  desc.textContent = item.description || "";
  const foot = document.createElement("div");
  foot.className = "market-card-foot";
  if (item.installed_source === "builtin" || item.updatable || item.installed_source === "user") {
    const badge = document.createElement("span");
    badge.className = "market-state-badge" + (item.updatable ? " updatable" : "");
    badge.textContent = item.updatable ? "可更新" : "内置";
    foot.append(badge);
  }
  const btn = document.createElement("button");
  btn.className = "market-action";
  if (marketBusyId === item.id) {
    btn.textContent = "下载中…";
    btn.disabled = true;
  } else if (item.installed_source === "builtin") {
    btn.textContent = "已内置";
    btn.disabled = true;
  } else if (item.installed_source === "user" && !item.updatable) {
    btn.textContent = "已安装";
    btn.disabled = true;
  } else {
    btn.textContent = item.updatable ? "更新" : "安装";
    btn.addEventListener("click", () => installSkin(item.id));
  }
  foot.append(btn);
  card.append(thumb, name, meta, desc, foot);
  return card;
}

function renderInstalledCard(skin) {
  const card = document.createElement("div");
  card.className = "market-card";
  const thumb = document.createElement("div");
  thumb.className = "market-card-thumb";
  applyThumbnail(thumb, skin.thumbnail);
  const name = document.createElement("span");
  name.className = "market-card-name";
  name.textContent = skin.name;
  const meta = document.createElement("small");
  meta.className = "market-card-meta";
  meta.textContent = [skin.author, skin.version].filter(Boolean).join(" · ");
  const foot = document.createElement("div");
  foot.className = "market-card-foot";
  if (skin.source !== "user") {
    const badge = document.createElement("span");
    badge.className = "market-state-badge";
    badge.textContent = "内置";
    foot.append(badge);
  } else {
    if (skin.id === currentSkinId) {
      const badge = document.createElement("span");
      badge.className = "market-state-badge current";
      badge.textContent = "使用中";
      foot.append(badge);
    }
    const del = document.createElement("button");
    del.className = "market-action danger";
    del.textContent = marketBusyId === skin.id ? "删除中…" : "删除";
    del.disabled = Boolean(marketBusyId);
    del.addEventListener("click", () => uninstallSkin(skin));
    foot.append(del);
  }
  card.append(thumb, name, meta, foot);
  return card;
}

async function installSkin(id) {
  if (!invoke || marketBusyId) return;
  marketBusyId = id;
  renderMarketView();
  try {
    await invoke("market_install", { id });
    showStatus("安装完成", "ok");
  } catch (e) {
    showStatus("安装失败: " + e, "err");
  } finally {
    marketBusyId = "";
    await loadMarket();
  }
}

async function uninstallSkin(skin) {
  if (!invoke || marketBusyId) return;
  const hint = skin.id === currentSkinId ? "该皮肤正在使用中，删除后将回落到默认皮肤。" : "";
  if (!confirm(`确定删除皮肤「${skin.name}」吗？${hint}此操作不可撤销。`)) return;
  marketBusyId = skin.id;
  renderMarketView();
  try {
    await invoke("market_uninstall", { id: skin.id });
    showStatus("已删除「" + skin.name + "」", "ok");
  } catch (e) {
    showStatus("删除失败: " + e, "err");
  } finally {
    marketBusyId = "";
    await loadMarket();
  }
}

el.marketBanner.addEventListener("click", () => showView("market"));
el.marketBack.addEventListener("click", () => showView("tools"));
el.marketRefresh.addEventListener("click", () => loadMarket());
el.marketTabs.addEventListener("click", (event) => {
  const tab = event.target.closest(".market-tab");
  if (!tab || tab.dataset.tab === marketTab) return;
  marketTab = tab.dataset.tab;
  for (const node of el.marketTabs.children) {
    node.classList.toggle("is-active", node === tab);
  }
  renderMarketView();
});
```

**按钮状态机小结**（市场页签）：无安装记录→「安装」；本机 user 版本更低→「更新」+「可更新」徽章；已装最新→「已安装」（禁用）；builtin 冲突→「已内置」（禁用）+「内置」徽章；任一操作进行中→「下载中…」（禁用）。安装/删除完成后统一 `loadMarket()` 重拉三个数据源并重绘。

- [ ] 6. 验证：`cd apps/overlay && cargo test && cargo build`（thumbnail 回改后测试须仍绿）。浏览器直开 `ui/index.html` 时 `invoke` 为空，市场页应停在空态不报错。

---

## Task 7：预置示例市场 + 本地联调 + 端到端验收

**新增文件**：`apps/overlay/ui/assets/market/market.json`（内置兜底清单）
**执行环境**：Windows PowerShell（打包与演示源）；演示远程源为可选项，内置清单本身就够离线验收。

- [ ] 1. 生成示例皮肤包（复用内置仓鼠资源，改 id 为 `lingxi-hamster-night`）。在仓库根执行：

```powershell
$stage = "$env:TEMP\lingxi-market-demo"
Remove-Item -Recurse -Force $stage -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force "$stage\lingxi-hamster-night" | Out-Null
Copy-Item -Recurse -Force "apps\overlay\ui\assets\skins\lingxi-hamster\*" "$stage\lingxi-hamster-night\"
# 改副本清单的 id / 名称，避免与内置皮肤冲突；字段名以 skin.json 实际结构为准
$manifest = Get-Content "$stage\lingxi-hamster-night\skin.json" -Raw | ConvertFrom-Json
$manifest.id = "lingxi-hamster-night"
$manifest.name = "仓鼠·夜色"
$manifest | ConvertTo-Json -Depth 10 | Set-Content "$stage\lingxi-hamster-night\skin.json" -Encoding UTF8
# 故意保留顶层目录打包 → 顺带验收 flatten_single_root 的兼容逻辑
Compress-Archive -Path "$stage\lingxi-hamster-night" -DestinationPath "$stage\lingxi-hamster-night-1.0.0.zip" -Force
Get-FileHash "$stage\lingxi-hamster-night-1.0.0.zip" -Algorithm SHA256
```

- [ ] 2. 编写内置兜底清单 `ui/assets/market/market.json`（`sha256` 用上一步 `Get-FileHash` 结果的小写形式替换；`size` 填 zip 实际字节数）：

```json
{
  "entries": [
    {
      "id": "lingxi-hamster-night",
      "name": "仓鼠·夜色",
      "author": "lingxi",
      "version": "1.0.0",
      "description": "基于默认仓鼠皮肤的演示包，用于验收下载与删除闭环",
      "thumbnail": "",
      "url": "http://127.0.0.1:8787/lingxi-hamster-night-1.0.0.zip",
      "sha256": "替换为第 1 步输出的 64 位小写十六进制",
      "size": 0
    }
  ]
}
```

- [ ] 3. （可选）本地模拟远程源：在演示目录起静态服务，并在用户配置里指向它（键名为 `market_url`，与 `BackendSettings` 字段的 serde 名一致）：

```powershell
Copy-Item "apps\overlay\ui\assets\market\market.json" "$stage\market.json"
python -m http.server 8787 --directory $stage
```

编辑 `%APPDATA%\lingxi\settings.json`，顶层加 `"market_url": "http://127.0.0.1:8787/market.json"`。远程与内置清单的 `id` 相同 → 顺带验收「远程优先」合并；把远程服务停掉再刷新市场 → 验收「静默降级」。

- [ ] 4. 端到端验收清单（逐条对应第二、三章定义）：

| # | 场景 | 通过标准 |
| --- | --- | --- |
| 1 | 冷启动 → 工具页 | 横幅不再禁用，徽章「进入」，点击进入市场页 |
| 2 | 市场页签 | 显示「仓鼠·夜色」卡片；断掉远程源仍可见（内置清单兜底） |
| 3 | 点「安装」 | 按钮变「下载中…」→ 完成后变「已安装」；出现于已安装页签 |
| 4 | 设置页联动 | 皮肤列表出现新皮肤（双源扫描），点击可切换、桌宠热换 |
| 5 | 完整性校验 | 手改清单 sha256 为错误值 → 报「文件校验失败，已取消安装」，无残留目录 |
| 6 | 升级 | version 改 1.0.1 重打包 → 按钮变「更新」+「可更新」徽章；升级使用中皮肤时桌宠自动刷新 |
| 7 | 使用中删除 | 已安装页签显示「使用中」徽章；confirm 提示回落；删除后桌宠立即回落仓鼠 |
| 8 | 非使用中删除 | confirm 后直接删除，列表即时消失，无广播 |
| 9 | 内置保护 | 内置皮肤无删除入口；清单含内置 id 时按钮为「已内置」 |
| 10 | 失败清理 | 安装中途失败后 `%APPDATA%\lingxi\skins` 下无 `.tmp-*` 残留 |
| 11 | 状态机一致性 | 操作进行中所有市场按钮禁用；完成后统一刷新三数据源 |

- [ ] 5. 收尾验证：`cd apps/overlay && cargo test && cargo build` 全绿；删除演示远程源配置（保留内置清单不影响日常使用）。

---

## 六、风险与对策

| # | 风险 | 影响 | 对策（已落实到本方案） |
| --- | --- | --- | --- |
| R1 | 远程拉取 / 下载阻塞 UI | 窗口冻结、无响应 | 三条市场命令全部 `#[tauri::command(async)]`，在独立线程执行；前端 `marketBusyId` 单飞状态机 + 「下载中…」即时反馈 |
| R2 | 版本号比较不完整 | 非常规版本号（如 `1.0.0-beta`）排序不准 | `version_rank` 采用逐段数值简化比较，覆盖「数值段升级」主流程；V2 可引入完整 semver 解析 |
| R3 | 无下载源签名 | 被篡改的远程源可能投放任意包 | sha256 只防传输损坏、不防恶意源；默认仅有内置清单这一可信源，`market_url` 须用户显式配置；V2 可增加签名校验 |
| R4 | 缩略图为外链 | 离线或外链失效时卡片无预览 | 加载失败仅显示色块占位，不阻塞卡片渲染；`sanitize_entry` 对非法缩略图只清空、不丢条目 |
| R5 | zip 打包结构差异 | 上传方是否带顶层目录不一 | `flatten_single_root` 自动上移唯一子目录；Task 7 演示包故意带顶层目录验收此逻辑 |
| R6 | 市场源内容维护成本 | 内置清单需人工更新 | V1 仅一个演示条目；清单格式公开（见 5.2 节），第三方可自建静态源（`market_url` 指向即可），零后端成本 |

## 七、后续迭代范围（V1 明确不做）

以下能力本期不实现，但接口设计已预留演进空间：

| 方向 | 说明 | V1 内的预留 |
| --- | --- | --- |
| 真正的插件形态 | V1 商品仅为皮肤包；JS 插件分发涉及沙箱与 API 稳定性 | 清单条目结构与商品类型无关，未来可扩展 `type: "plugin"` 字段 |
| 市场内动图预览 | 需额外资源与播放控件 | `thumbnail` 已兼容雪碧图格式，动图可作为新增字段平滑加入 |
| 本地皮肤全量管理 | 已安装页签仅列「来自市场」的皮肤 | `installed_source` 已区分来源，扩展枚举即可纳入全部本地皮肤 |
| 增量 / 差分更新 | 需服务端分片支持 | 每次全量下载覆盖安装，`sha256` 保证一致性 |
| 多市场源管理 UI | V1 仅单远程源 + 内置源 | `market_url` 为单值字段，多源只需改列表化并调整合并逻辑 |
| 下载统计 / 评论 | 需后端服务 | 清单为纯静态 JSON，天然无状态 |

## 八、自审核对清单

实现完成后逐项核对（实现者自查 + 评审者抽查）：

- [ ] A1. `main.rs` 命令注册三条齐全：`market::market_list, market::market_install, market::market_uninstall,`（与既有 `pet_skin::…` 注册风格一致）。
- [ ] A2. `Cargo.toml` 三依赖齐全且版本匹配：`ureq = "2"`、`zip = { version = "2", default-features = false, features = ["deflate"] }`、`sha2 = "0.10"`。
- [ ] A3. `tauri.conf.json` 已加 `app.security.assetProtocol: { "enable": true, "scope": ["$APPDATA/lingxi/skins/**"] }`；CSP `img-src` 无需改动（已含 `asset:`）。
- [ ] A4. `market.rs` 中 `MarketEntry` / `MarketItem` 均含 `thumbnail` 字段；`sanitize_entry` 对非法缩略图只清空不丢条目；`market_list` 映射处已透传该字段。
- [ ] A5. 三条市场命令均带 `#[tauri::command(async)]` 属性（防止主线程冻结窗口）。
- [ ] A6. 全文扫描无占位符：无 `TODO`、无省略号代码、无「此处略」；示例 `sha256` 为显式的「替换为…」说明文字而非伪代码。
- [ ] A7. `market_uninstall` 对内置皮肤拒绝删除；对使用中皮肤先回落再删除，且回落失败中止删除（`switch_to_default_skin` 内 `view_for` 以 `?` 上抛）。
- [ ] A8. 前端 `el` 映射 9 项齐全；`showView` 三处小改（hidden / map / loadMarket）；`marketBusyId` 在成功与失败路径都会复位。
- [ ] A9. `settings.json` 键名为 `market_url`（snake_case，与无 `rename_all` 的 serde 默认命名一致）。
- [ ] A10. 文档引用的既有代码锚点（横幅 HTML、el 映射、showView、renderSkinGrid、CSP）若实现时行号漂移，以内容描述为准。

## 九、执行交接

本方案已按 bite-sized 任务拆解为 7 个任务，每个任务含完整代码与验证命令，可独立执行与验证：

- **推荐执行顺序**：Task 1（依赖与配置）→ Task 2（下载与哈希基础）→ Task 3（market.rs 清单与命令）→ Task 4（skin_source 双源扫描）→ Task 5（卸载与回落）→ Task 6（前端闭环）→ Task 7（示例市场与端到端验收）。
- **依赖关系**：Task 3 依赖 Task 2（下载与哈希工具）；Task 5 依赖 Task 4（`skin_source`）；Task 6 依赖 Task 3-5（命令可用）；Task 7 依赖全部。
- **独立验证**：各任务末尾均附 `cargo test` / `cargo build` 或浏览器手动验证点，全部通过后再进入下一任务。

