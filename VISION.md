# 灵犀（LingXi）愿景规划

> 版本：v2.0
> 日期：2026-08-11
> 状态：长期愿景文档，具体任务清单见 `ROADMAP.md`

---

## 一、定位升级

**灵犀 = 常驻桌面的智能运行时 + 实用工具站。**

- 对任何输入法，它是 **AI 能力外挂**（弥补输入法插件的不足）；
- 对用户，它是 **跨应用桌面 Agent**（选中即行动，输入即上下文）；
- 对日常效率，它是 **桌面小工具集合**（屏幕识别、全屏翻译、天气、取色…即开即用）；
- 对开发者，它是 **插件库**（一个能力注册一次，输入法、Agent、小工具三处共用）。

---

## 二、核心洞察：一个底座 · 四个角色

输入法增强、桌面 Agent、桌面小工具、插件库不是四条产品线，而是同一个"常驻桌面智能运行时"的四个面向。

```
用户触达：  输入法增强       桌面 Agent        桌面小工具         插件库生态
              │                 │                  │                  │
              └────────┬────────┴──────────┬───────┴────────┬─────────┘
                       ▼
            灵犀智能运行时 · 统一底座
        上下文感知 · 工具注册表 · Agent 引擎 · 模型后端 · 权限门控
                       │
                       ▼
            生态连接层（OwO/rime · QQ/浏览器/IDE · 第三方插件）
```

底层能力完全重叠（UIA 上下文感知、键盘/剪贴板、屏幕捕获、模型推理、权限门控），共享底座，只换交互入口。**每一次 AI 调用都携带上下文（当前输入、当前应用、当前选区、当前屏幕）**，这是与所有单场景工具的本质区别。

---

## 三、输入法插件角色：弥补输入法的不足

不取代 OwO/rime 等真 TSF 输入法（打字底座归它们），灵犀专注它们做不到的部分：

| 输入法插件的痛点 | 灵犀的弥补方式 |
|---|---|
| 候选只看拼音，无语义 | 读光标前文 → LLM 语义候选、短语联想（扩展 `assistant-ime` rerank 雏形） |
| 不知道你在写邮件还是聊天 | UIA 识别前台应用 → 场景化语气 / 词库切换 |
| 插件 API 只能改候选 | 输入法通过 IPC 调用灵犀 = 获得完整 Agent 工具链 |
| 不能执行动作 | 选词后候选窗直接给出"翻译 / 搜索 / 查代码"等动作 |
| 单机无记忆 | Agent 会话持久化，记住用户用词偏好 |

**协作模式**：与 OwO 平级 IPC 协作（见 `docs/owopkg-plugin-feasibility.md` 结论 A+C），同时将 `plugins/lingxi-rime-filter` 实验转正。

---

## 四、桌面小工具角色：即开即用的效率面板（新增重点）

**这是 v2.0 相对 v1.0 最大的增量。** 用户不需要打开对话框、不需要写 prompt，按一个快捷键或点一下桌宠菜单，就能用某个具体功能。

### 4.1 设计原则

- **独立窗口**：每个小工具是一个独立的小窗口（非主浮窗的子页），可独立移动、置顶、关闭；
- **即开即用**：快捷键 / 托盘菜单 / 桌宠右键菜单触发，秒开秒用，用完即关；
- **共享底座**：小工具复用 ToolRegistry 的能力（OCR 工具同时服务 Agent 和屏幕识别小工具）；
- **风格统一**：所有小工具遵循同一套设计语言（毛玻璃 + 圆角 + 紧凑信息密度）。

### 4.2 第一批桌面小工具

| 小工具 | 触发方式 | 能力 | 底层依赖 |
|---|---|---|---|
| **屏幕识别 (OCR)** | Ctrl+Alt+O / 托盘 | 框选屏幕区域 → OCR 提取文字 → 可复制/翻译/搜索 | `capture_screen` + OCR 模型 |
| **全屏翻译** | Ctrl+Alt+T / 托盘 | 框选区域 → 识别文字 → 原位叠加翻译层（不遮挡原文） | OCR + `translate` 工具 |
| **取色器** | Ctrl+Alt+C | 放大镜 + 取色，HEX/RGB/HSL，历史色板 | 屏幕像素读取 |
| **天气** | 托盘 / 桌宠菜单 | 当前天气 + 3 日预报 + 桌宠状态联动（下雨桌宠打伞） | `web_fetch` + 天气 API |
| **剪贴板历史** | Ctrl+Alt+V | 最近 N 条剪贴板，搜索/置顶/收藏，支持图片 | `clipboard` 工具扩展 |
| **截图标注** | Ctrl+Alt+S | 框选 → 箭头/方框/文字/马赛克 → 复制/保存 | 屏幕捕获 + Canvas |
| **悬浮计算器** | Ctrl+Alt+= | 输入即算（支持单位换算、汇率、变量），结果悬浮显示 | `calculate` 工具 |
| **单位换算** | 托盘 | 长度/重量/温度/汇率，离线 + 在线汇率 | `calculate` 扩展 |

### 4.3 小工具的插件化

每个小工具本身就是一个**插件**（遵循统一的 manifest 规范），用户可以：
- 启用 / 禁用任意小工具（禁用后快捷键和菜单项消失）；
- 调整快捷键绑定；
- 从插件市场安装第三方小工具（例如：番茄钟、屏幕标尺、DNS 切换…）。

### 4.4 小工具 UI 规范

```
┌─────────────────────────────┐
│  [图标] 小工具名        ─ ✕  │  ← 28px 标题栏，毛玻璃，可拖动
├─────────────────────────────┤
│                             │
│      主体内容区              │  ← 紧凑信息密度，最大 480×320
│                             │
├─────────────────────────────┤
│  状态提示            [操作]  │  ← 可选底栏
└─────────────────────────────┘
```

- 圆角 12px，毛玻璃背景（Windows 11 Mica / 亚克力）；
- 字体：标题 14px medium，正文 13px，辅助 11px；
- 交互：Esc 关闭，Ctrl+C 复制结果，所有按钮有 hover/active 反馈。

---

## 五、插件库角色：统一的能力注册（v2.0 重构）

插件 = 一段"能力声明 + 实现"，注册进 ToolRegistry 即被 **输入法、Agent、小工具** 三处共用。

### 5.1 插件类型（六类）

| 类型 | 例子 | 运行时 | 入口 |
|---|---|---|---|
| 能力插件 | OCR、语音输入、翻译引擎、剪贴板历史 | 原生 Rust | Agent 工具 + 小工具 |
| 应用适配插件 | QQ、微信、飞书、浏览器、IDE、Office | 原生 + 配置 | Agent 工具 |
| 变换插件 | 润色、纠错、格式化、摘要 | 原生（后续可脚本） | 输入法候选 + Agent |
| 信息源插件 | 搜索、天气、日程、知识库 | 原生 / 远程 | Agent 工具 + 小工具 |
| 输入后端插件 | TSF 注册、OwO 桥、rime 桥 | 原生 | 输入法 |
| **桌面小工具插件（新增）** | 截图标注、取色器、悬浮计算器、屏幕标尺 | 原生 + Webview | 独立窗口 / 快捷键 |

### 5.2 阶段 2 技术选型（已定）

- **先原生插件**：Rust 编译进系统 + `manifest.json` 声明式加载，风险最低、性能最好；
- **小工具用 Tauri 多窗口**：每个小工具开一个 `WebviewWindow`，共享 Rust 后端，前端用轻量 HTML/CSS/JS；
- 脚本插件（JS/Python）作为后续增强，不阻塞第一阶段。

### 5.3 插件契约

- **manifest.json 声明式**：
  ```json
  {
    "name": "screen-ocr",
    "version": "1.0.0",
    "type": "widget",           // capability | adapter | transform | source | input_backend | widget
    "permissions": ["screen_capture", "clipboard_write"],
    "tools": ["capture_screen", "ocr_recognize"],
    "widget": {                 // 仅 type=widget 时存在
      "title": "屏幕识别",
      "shortcut": "Ctrl+Alt+O",
      "size": [420, 280],
      "resizable": true,
      "page": "widgets/ocr.html"
    }
  }
  ```
- **权限沿用三档风险模型**（Safe / Moderate / Dangerous），安装时按权限向用户确认；
- **一个能力三处入口**：插件工具既出现在 Agent 对话工具列表，也出现在输入法候选窗的"动作"里，还能被小工具直接调用。

---

## 六、工具界面美化方案（v2.0 重构）

当前工具页（`tools-view`）是朴素的纵向列表，信息密度低、视觉单调。v2.0 重构为**卡片网格 + 分类 + 搜索 + 插件市场入口**。

### 6.1 新工具页布局

```
┌──────────────────────────────────────────┐
│  🔍 搜索工具...              [插件市场]   │  ← 搜索栏 + 市场入口
├──────────────────────────────────────────┤
│  [全部] [能力] [小工具] [应用] [信息源]   │  ← 分类筛选 tab
├──────────────────────────────────────────┤
│  ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ [图标]  │ │ [图标]  │ │ [图标]  │       │  ← 卡片网格 2-3 列
│  │ 屏幕识别│ │ 全屏翻译│ │ 取色器  │       │
│  │ Ctrl+O │ │ Ctrl+T │ │ Ctrl+C │       │
│  │  ●启用 │ │  ○禁用 │ │  ●启用 │       │
│  └────────┘ └────────┘ └────────┘        │
│  ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ ...    │ │ ...    │ │ ...    │        │
│  └────────┘ └────────┘ └────────┘        │
└──────────────────────────────────────────┘
```

### 6.2 卡片设计

- **尺寸**：自适应宽度，最小 140px，高度约 88px；
- **内容**：图标（emoji 或 SVG）+ 名称 + 快捷键 + 启用开关 + 风险标签；
- **交互**：hover 上浮 + 边框高亮；点击卡片打开插件详情（描述、权限、配置、卸载）；
- **视觉**：毛玻璃背景，圆角 10px，禁用态 45% 透明度 + 灰度。

### 6.3 插件市场（阶段 3）

工具页右上角"插件市场"按钮 → 打开独立窗口：
- 浏览 / 搜索 / 安装 / 更新 / 卸载；
- 本地目录 + GitHub 源拉取；
- 安装时显示权限清单，用户确认后注册。

---

## 七、四阶段路线图（v2.0 调整）

> **v2.0 关键变化**：把原"阶段 2 插件框架"拆分——**小工具框架提前到阶段 1**（与输入法增强并行），**插件市场推迟到阶段 3**。理由：用户更需要"即开即用的小工具"，而非等输入法增强完成后才动手。

| 阶段 | 主题 | 关键交付物 |
|---|---|---|
| 0 | Agent 底座补全（当前，收尾中） | 工具集补全 · 确认门控 UI · 会话持久化 · 光标前文上下文读取 |
| 1 | **桌面小工具 + 输入法增强**（并行） | 小工具框架 · 首批 8 个小工具 · 工具页美化 · AI 语义候选 · OwO IPC |
| 2 | 插件框架正式化 | manifest 规范 + 加载器 · 第三方插件接入 · 脚本运行时 |
| 3 | 生态分发 | NSIS 一键安装 · 插件商店 · 词库/偏好云同步 |

详细任务清单见 `ROADMAP.md`。

---

## 八、差异化护城河

现有产品只占其中一角：

| 产品 | 占据的角 | 灵犀的差异 |
|---|---|---|
| 搜狗 / 微软 AI 输入 | 输入联想 | 灵犀有 Agent + 小工具 + 插件生态 |
| Raycast / Alfred | 命令面板 | 灵犀有输入法上下文 + 常驻桌宠 |
| utools / Rubick | 插件市场 | 灵犀有输入法增强 + AI Agent |
| Snipaste / ShareX | 截图标注 | 灵犀小工具是 AI 增强的（截图 → OCR → 翻译 → 搜索一条龙） |
| Pixel Ace / 取色器 | 单一工具 | 灵犀是工具集合，不占多个安装位 |

**"输入时 AI 就在上下文里 + 选中即 Agent + 即开即用的小工具 + 生态可插拔"** 是组合优势，不与任何单一产品正面竞争。

---

## 九、与现有代码的衔接

- `crates/lingxi-agent`（Agent 引擎）、`crates/tools`（Tool trait + Registry）、`crates/tools-windows`（Windows 工具）已是统一底座的核心，规划是对其的系统化扩展，不是重写；
- `apps/overlay` 的对话视图、工具视图、选区视图即为三个角色的 UI 入口，小工具将新增为独立的 `WebviewWindow`；
- `assistant-windows` 的 UIA / 键盘 / 剪贴板 / 屏幕捕获能力是上下文感知、动作执行与小工具的基础，保持复用；
- **工具页美化**只改 `apps/overlay/ui/index.html` + `styles.css` + `app.js` 的工具视图部分，不影响其他视图。
