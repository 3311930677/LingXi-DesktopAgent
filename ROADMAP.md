# 灵犀（LingXi）路线图

> 对齐 `VISION.md` v2.0 的四阶段路线：
> 阶段 0 底座补全 → **阶段 1 桌面小工具 + 输入法增强** → 阶段 2 插件框架正式化 → 阶段 3 生态分发。
> 本文件是任务清单；设计理念与长期定位见 `VISION.md`。

---

## 阶段 0：Agent 底座补全（收尾中）

把"统一底座"做成可信赖的运行时：工具集补全、确认门控 UI、会话持久化、上下文读取。

### 0.1 Agent 引擎与工具框架 ✅ 已完成
- [x] `crates/lingxi-agent` — AgentEngine + AgentBackend + Session + ReAct 循环
- [x] `crates/tools` — Tool trait + ToolRegistry + schema/context/result 类型
- [x] `assistant-inference` 扩展 CloudAgentBackend (function calling)
- [x] 单元测试：Mock 工具 + Mock backend 验证对话闭环
- [x] `crates/tools-windows` — 包装 assistant-windows 为工具（clip / file / qq / shell / uia）

### 0.2 UI 与会话 ✅ 基本完成
- [x] 对话面板视图（apps/overlay ui）
- [x] 工具管理视图（列表 / 启用 / 禁用）
- [x] 确认门控 UI（三档风险）
- [x] 会话 JSON 持久化（agent-session.json，跨重启恢复 + 历史裁剪）
- [ ] 会话存储升级 SQLite + 上下文窗口管理（滑动窗口 + 摘要压缩防 token 爆炸）

### 0.3 Windows 工具集补全 ✅ 基本完成（2026-08-11）
- [x] `window_tools`：list_windows / focus_window
- [x] `app_tools`：open_app
- [x] `input_tools`：type_text / send_keys / click_at
- [x] `search_files`：按名称搜索文件
- [x] P0/P1 bug 修复：session 丢失 / Ctrl+Enter 双触发 / Escape 关浮窗 / 命令超时 / 大文件 OOM / Mutex 毒化 / 编码乱码（2026-08-11）

### 0.4 信息工具与后台任务 ✅ 基本完成
- [x] web_fetch / translate / calculate / get_time
- [x] web_search（DuckDuckGo HTML）
- [x] 提醒系统 set_reminder / list_reminders / cancel_reminder
- [ ] 后台任务调度器（定时 / 事件触发）

### 0.5 上下文感知 ⏳ 待办（收尾项）
- [ ] 光标前文读取（当前输入框已有文本 → Agent / 输入法上下文）
- [ ] 前台应用场景识别（聊天 / 邮件 / 代码 / 文档）
- [ ] `capture_screen` 工具正式注册（稳定图片回传契约，为阶段 1 OCR 小工具铺路）

---

## 阶段 1：桌面小工具 + 输入法增强（下一关键里程碑，并行推进）

**v2.0 关键变化**：原阶段 1 只有输入法增强，现在加入桌面小工具框架，两条线并行。小工具是用户最直接感知的价值，优先级最高。

### 1A 桌面小工具框架 ⭐ 最高优先级

#### 1A.0 屏幕捕获基础设施（小工具的前置依赖）
- [ ] `capture_screen` 工具正式实现（Win32 BitBlt / DXGI Desktop Duplication）
- [ ] 屏幕区域选择 overlay（全屏半透明遮罩 + 框选，类似 Snipaste）
- [ ] 像素读取 API（为取色器铺路）
- [ ] 图片在 Rust ↔ 前端之间的高效回传（base64 / 共享内存）

#### 1A.1 小工具窗口框架
- [ ] Tauri 多窗口管理器（动态创建/销毁 `WebviewWindow`）
- [ ] 小工具注册表（从 manifest 声明，注册到 ToolRegistry）
- [ ] 全局快捷键注册器（每个小工具独立快捷键，可配置）
- [ ] 托盘菜单动态生成（已启用的小工具自动出现在菜单）
- [ ] 桌宠右键菜单集成（小工具入口）
- [ ] 小工具公共 UI 组件库（标题栏 / 毛玻璃 / Esc 关闭 / 拖动）

#### 1A.2 首批 8 个小工具

##### ① 屏幕识别 (OCR) — `Ctrl+Alt+O`
- [ ] 框选屏幕区域 → 截图
- [ ] OCR 引擎接入（优先 Windows.Media.Ocr，备选 tesseract / 在线 API）
- [ ] 结果面板：识别文字 + 复制 + 翻译 + 搜索按钮
- [ ] 历史记录（最近 10 次识别）

##### ② 全屏翻译 — `Ctrl+Alt+T`
- [ ] 框选区域 → OCR 提取文字
- [ ] 调用 `translate` 工具翻译
- [ ] 原位叠加翻译层（半透明覆盖，不遮挡原文，hover 切换）
- [ ] 语言对自动检测 + 手动指定

##### ③ 取色器 — `Ctrl+Alt+C`
- [ ] 全屏放大镜（跟随鼠标，4x / 8x）
- [ ] 实时显示当前像素 HEX / RGB / HSL
- [ ] 取色 → 写入剪贴板（可选格式）
- [ ] 历史色板（最近 20 色，点击复制）

##### ④ 天气 — 托盘/桌宠菜单
- [ ] 位置获取（IP 定位 / 手动输入城市）
- [ ] 天气 API 接入（Open-Meteo 免费无密钥 / 和风天气）
- [ ] 当前天气卡片 + 3 日预报
- [ ] 桌宠状态联动（晴天/雨天/雪天 → 桌宠不同表情/道具）

##### ⑤ 剪贴板历史 — `Ctrl+Alt+V`
- [ ] 剪贴板监听器（文本 + 图片）
- [ ] 历史列表（搜索 / 置顶 / 收藏 / 删除）
- [ ] 持久化（SQLite，跨重启保留）
- [ ] 点击条目 → 写入剪贴板并粘贴到当前输入框

##### ⑥ 截图标注 — `Ctrl+Alt+S`
- [ ] 框选 → 进入标注模式
- [ ] 标注工具：箭头 / 方框 / 圆形 / 文字 / 马赛克 / 画笔
- [ ] 撤销/重做
- [ ] 复制到剪贴板 / 保存到文件

##### ⑦ 悬浮计算器 — `Ctrl+Alt+=`
- [ ] 输入即算（调用 `calculate` 工具）
- [ ] 支持单位换算（长度/重量/温度）
- [ ] 汇率换算（在线获取汇率）
- [ ] 变量定义（`x = 10`，后续 `x * 2`）

##### ⑧ 单位换算 — 托盘
- [ ] 长度/重量/温度/面积/体积/速度
- [ ] 离线换算表 + 在线汇率
- [ ] 常用换算置顶

#### 1A.3 小工具通用能力
- [ ] 小工具设置页（启用/禁用 / 快捷键配置 / 位置记忆）
- [ ] 小工具间联动（OCR 结果 → 翻译；截图 → OCR；剪贴板 → 计算器）

### 1B 工具页美化（与 1A 并行）

#### 1B.1 工具页重构
- [ ] 从纵向列表改为卡片网格（2-3 列响应式）
- [ ] 搜索栏（按名称/描述模糊搜索）
- [ ] 分类筛选 tab（全部 / 能力 / 小工具 / 应用 / 信息源 / 变换）
- [ ] 插件市场入口按钮（阶段 3 激活）

#### 1B.2 卡片视觉升级
- [ ] 图标系统（emoji 优先，备选 SVG icon set）
- [ ] 卡片 hover 动效（上浮 + 边框高亮）
- [ ] 启用/禁用开关美化（toggle switch 而非 checkbox）
- [ ] 风险标签彩色化（Safe 绿 / Moderate 橙 / Dangerous 红）
- [ ] 快捷键显示（`Ctrl+Alt+O` 格式）
- [ ] 点击卡片 → 展开详情面板（描述 / 权限 / 配置 / 卸载）

#### 1B.3 整体视觉语言统一
- [ ] 毛玻璃背景（Windows 11 Mica / 亚克力效果）
- [ ] 统一圆角（卡片 10px / 面板 12px / 按钮 8px）
- [ ] 统一间距（8/12/16/20px 栅格）
- [ ] 暗色模式优化（当前已有暗色，需调对比度）

### 1C 输入法增强（与 1A 并行，优先级略低）

- [ ] AI 语义候选：读光标前文 → LLM 候选 / 短语联想（扩展 `assistant-ime` rerank）
- [ ] AI 续写 / 接龙：候选窗"帮我把这句写完"
- [ ] 动作型候选：选中词 → 候选窗给出翻译 / 搜索 / 查代码
- [ ] 场景化词库：按前台应用切换语气 / 词库
- [ ] OwO IPC 协作协议：输入法 ↔ 灵犀 平级协作
- [ ] `plugins/lingxi-rime-filter` 实验转正，接入主运行链路
- [ ] 候选提交从"受控剪贴板 + Ctrl+V"逐步过渡到更可靠的写回方式

---

## 阶段 2：插件框架正式化

开放能力注册，形成插件库，让第三方能开发插件。

### 2.1 manifest 规范与加载器
- [ ] manifest schema 定稿：`{name, version, type, permissions[], tools[], widget?}`
- [ ] 本地插件加载器：扫描 `~/.lingxi/plugins/` + 内置插件目录
- [ ] 插件生命周期管理（加载 / 启用 / 禁用 / 卸载 / 更新）
- [ ] 插件依赖解析（A 依赖 B 的工具）

### 2.2 权限与安全
- [ ] 插件权限声明与安装确认（沿用三档风险模型）
- [ ] 插件沙箱（限制文件系统 / 网络访问范围）
- [ ] 插件签名验证（可选，阶段 3 商店强制）

### 2.3 插件开发体验
- [ ] 插件脚手架 CLI（`lingxi plugin new <name>`）
- [ ] 插件开发文档 + 示例插件
- [ ] 插件调试模式（热重载 / 日志查看）
- [ ] 脚本插件运行时（JS / Python）作为后续增强

### 2.4 六类插件接入
- [ ] 能力插件（OCR / 语音 / 翻译引擎）
- [ ] 应用适配插件（QQ / 微信 / 飞书 / 浏览器 / IDE）
- [ ] 变换插件（润色 / 纠错 / 格式化 / 摘要）
- [ ] 信息源插件（搜索 / 天气 / 日程 / 知识库）
- [ ] 输入后端插件（TSF / OwO 桥 / rime 桥）
- [ ] 桌面小工具插件（第三方小工具）

---

## 阶段 3：生态分发

- [ ] NSIS 一键安装包（输入法 + Agent + 桌宠 + 插件框架整体捆绑）
- [ ] 插件商店（浏览 / 搜索 / 安装 / 更新 / 评分 / 卸载）
- [ ] 插件商店后端（GitHub 源 + 自建索引）
- [ ] 用户词库与偏好云同步
- [ ] 社区贡献模板（插件脚手架 + 文档模板）
- [ ] 插件签名机制（商店上架强制签名）

---

## 里程碑对照

| 阶段 | 完成标志 |
|---|---|
| 0 | Agent 能通过自然语言操作 Windows 应用 + 记住会话 + 读到输入上下文 |
| 1A | 用户能用快捷键打开 8 个桌面小工具，工具页是美观的卡片网格 |
| 1B | 在任何输入框打字时，候选窗出现 AI 语义候选与动作候选；OwO 可调用灵犀能力 |
| 2 | 第三方能独立开发一个插件并在灵犀中安装运行 |
| 3 | 新用户一键安装即可获得完整"输入 + Agent + 小工具 + 插件"体验 |

---

## 优先级排序（阶段 1 内部）

> 阶段 1 三条线并行，但资源有限时按此序投入：

1. **1A.0 屏幕捕获基础设施** — 所有视觉小工具的前置依赖
2. **1A.1 小工具窗口框架** — 让小工具能跑起来
3. **1A.2① 屏幕识别 OCR** — 最高频刚需，展示 AI 增强小工具的价值
4. **1B.1-1B.2 工具页美化** — 快速提升视觉感知，用户立即可见
5. **1A.2④ 天气** — 简单但有趣，展示桌宠联动
6. **1A.2⑤ 剪贴板历史** — 高频刚需
7. **1A.2③ 取色器** — 简单独立，验证像素读取
8. **1A.2② 全屏翻译** — 依赖 OCR，排在其后
9. **1A.2⑥ 截图标注** — 工作量最大，排后
10. **1A.2⑦⑧ 计算器/单位换算** — 已有 calculate 工具，包装即可
11. **1C 输入法增强** — 重要但不紧急，等小工具框架稳定后推进
