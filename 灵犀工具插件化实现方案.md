# 灵犀工具插件化实现方案（方案 A：命令/脚本型工具插件）

> **执行者须知：** 按任务顺序执行，每个任务内步骤串行完成。步骤使用 `- [ ]` 复选框跟踪。

**目标：** 让 Agent 工具像皮肤一样可从市场安装/更新/卸载——工具插件 = 目录 + `tool.json` 清单 + 脚本，安装后自动注册进工具注册表，Agent 对话即可调用。

**架构：** 复用插件市场已建好的下载→SHA256 校验→安全解压→落地→回滚链路，新增 `kind` 字段区分「皮肤/工具」两类商品。工具执行器 `PluginTool` 实现 `Tool` trait：以子进程拉起插件声明的命令，参数 JSON 走 stdin，stdout 作为工具输出，超时自动杀死。注册表同步：启动时扫描 + 安装/卸载后全量重建。

**技术栈：** Rust（lingxi-tools crate 新增 `plugin` 模块，tokio::process 子进程执行）+ 前端原生 JS 市场页扩展。

---

## 设计要点

### 工具插件包结构

```
<插件id>/
├── tool.json     # 清单（见下）
└── main.py       # 命令的工作目录内任意资源文件
```

zip 顶层保留插件目录（与皮肤包一致，`flatten_single_root` 归一化）。

### tool.json 清单格式

```json
{
  "id": "lingxi-tool-upper",
  "name": "upper_text",
  "display_name": "文本大写",
  "author": "lingxi",
  "version": "1.0.0",
  "description": "把输入文本转成大写字母",
  "risk_level": "safe",
  "command": ["python", "main.py"],
  "parameters": {
    "type": "object",
    "properties": { "text": { "type": "string", "description": "要转换的文本" } },
    "required": ["text"]
  },
  "timeout_secs": 15
}
```

| 字段 | 规则 |
|---|---|
| `id` | 目录名/市场条目 id，`[A-Za-z0-9_-]`，≤64 |
| `name` | 注册进 `ToolRegistry` 的工具名（LLM 调用名），`^[a-z0-9][a-z0-9_]*$`，≤64，全局唯一 |
| `risk_level` | `safe`/`moderate`/`dangerous`；**缺省 moderate**（第三方代码默认多一分谨慎） |
| `command` | 非空数组；`command[0]` 禁止路径分隔符/绝对路径（只能引 PATH 中的解释器，如 python/node），防 `..\evil.exe` |
| `timeout_secs` | 1..=300，缺省 30 |
| `parameters` | JSON Schema（OpenAI function-calling 格式），缺省空对象 schema |

### 执行模型

`PluginTool::execute(params)`：
1. `tokio::process::Command` 在**插件目录**内拉起 `command`，`kill_on_drop(true)`
2. params JSON 序列化写入子进程 **stdin** 后关闭（无命令行转义问题）
3. `tokio::time::timeout` 包裹 `wait_with_output()`，超时返回错误（drop 自动杀进程）
4. 退出码 0 → stdout（UTF-8 lossy，截断 64KB）为 `ToolResult::ok`；非 0 → stderr 前 2KB 作错误信息

### 安全模型（与皮肤市场的关键差异）

- `safe_extract` 黑名单按 kind 差异化：
  - **皮肤**（现状）：禁 `exe dll bat cmd ps1 vbs scr msi com`（皮肤不该有可执行内容）
  - **工具**：仍禁原生二进制 `exe dll vbs scr msi com`，**放行 `bat cmd ps1`**（脚本是工具插件的交付物）
- 工具名冲突保护：安装前检查 `name` 是否被内置工具或其他插件占用（自身升级除外）
- `command[0]` 无路径分隔符 → 插件包本身无法携带原生可执行文件来执行
- 危险级别沿用注册表确认门：`dangerous` 工具每次调用需确认

### 注册表同步（两处装配点）

| 场景 | 动作 |
|---|---|
| 启动 | `main.rs` setup 调 `market::sync_plugin_tools(&state)`：按 `plugin_tool_map` 移除旧项 → 重扫注册 |
| 市场安装/卸载工具 | 命令成功后调 `sync_plugin_tools`（或精准移除） |
| `agent_chat` | 每轮**重建**注册表（现状）：`register_default_tools` 后同样扫描插件目录注册 |
| UI `list_tools` | 从 `state.plugin_tool_map` 判断 `source: "plugin"/"builtin"` |

`AppState` 新增 `plugin_tool_map: Mutex<HashMap<String /*插件id*/, String /*工具名*/>>`——记录注册表里哪些名字来自插件，卸载/同步时据此移除。

---

## 环境注意事项（本机实测坑，务必遵守）

1. **同一文件多处修改必须串行执行**：本环境并行编辑同一文件会互相覆盖且报成功。每个 SearchReplace 单独发，改完用 `Select-String` 复核磁盘再进行下一个。
2. **关键修改后 shell 复核**：`Select-String -Path <file> -Pattern "<标记>"` 确认真落盘。
3. **JSON 文件一律用 Write 工具写**（UTF-8 无 BOM）；PowerShell 5.1 的 `Set-Content -Encoding UTF8` 带 BOM，serde_json 会解析失败。
4. **新编译 exe 可能被 WDAC 拦截**（"Application Control policy"）：重跑一次 `cargo build` 换哈希即可绕开。
5. `apps/overlay` 有**独立 Cargo.lock**，根 workspace `exclude` 了它——overlay 的 cargo 命令必须在 `apps\overlay` 目录下跑。
6. 演示物料放 `apps\overlay\target\market-demo\`（构建目录不入库）；清单服务器 `python -m http.server 8787`；`settings.json` 已配置 `market_url` 指向本地。

---

## Task 1：lingxi-tools 插件基础设施（registry.remove + 清单解析）

**Files:**
- Modify: `crates/tools/Cargo.toml`（tokio 由 optional 转正式依赖）
- Modify: `crates/tools/src/lib.rs`（导出 plugin 模块）
- Modify: `crates/tools/src/registry.rs`（新增 remove）
- Create: `crates/tools/src/plugin.rs`（清单结构 + 解析校验）

- [ ] **步骤 1：写失败测试**——`crates/tools/src/plugin.rs` 新建（先只含测试与空实现骨架）：

```rust
//! 工具插件：tool.json 清单解析与命令行执行器。

use crate::context::RiskLevel;
use crate::schema::{ToolResult, ToolSchema};
use crate::Tool;
use async_trait::async_trait;
use serde::{Deserialize, Serialize};
use std::path::{Path, PathBuf};
use std::time::Duration;

/// 清单文件名。
pub const MANIFEST_FILE: &str = "tool.json";
/// 默认执行超时（秒）。
const DEFAULT_TIMEOUT_SECS: u64 = 30;
/// 最大执行超时（秒）。
const MAX_TIMEOUT_SECS: u64 = 300;

/// 工具插件清单（tool.json）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PluginManifest {
    /// 插件目录名（安装包 id，与市场条目一致）。
    pub id: String,
    /// 注册进工具注册表的工具名（LLM 调用名，全局唯一）。
    pub name: String,
    /// 市场卡片展示名；缺省回落 name。
    #[serde(default)]
    pub display_name: String,
    #[serde(default)]
    pub author: String,
    pub version: String,
    #[serde(default)]
    pub description: String,
    /// safe | moderate | dangerous；缺省 moderate（第三方代码默认多一分谨慎）。
    #[serde(default = "default_risk")]
    pub risk_level: String,
    /// 子进程命令行：command[0] 必须是无路径分隔符的程序名。
    pub command: Vec<String>,
    /// 参数 JSON Schema（发给 LLM）。
    #[serde(default = "default_parameters")]
    pub parameters: serde_json::Value,
    #[serde(default)]
    pub timeout_secs: Option<u64>,
}

fn default_risk() -> String {
    "moderate".into()
}

fn default_parameters() -> serde_json::Value {
    serde_json::json!({"type": "object", "properties": {}})
}

/// 解析并校验清单文本。
pub fn parse_manifest(text: &str) -> Result<PluginManifest, String> {
    let manifest: PluginManifest =
        serde_json::from_str(text).map_err(|error| format!("tool.json 格式错误：{error}"))?;
    validate_manifest(&manifest)?;
    Ok(manifest)
}

/// 插件目录 id：与皮肤 id 同规则（[A-Za-z0-9_-]，≤64）。
pub fn valid_plugin_id(id: &str) -> bool {
    !id.is_empty()
        && id.len() <= 64
        && id.chars().all(|c| c.is_ascii_alphanumeric() || c == '-' || c == '_')
}

/// 工具注册名：小写字母数字下划线（LLM 函数名惯例），≤64。
pub fn valid_tool_name(name: &str) -> bool {
    !name.is_empty()
        && name.len() <= 64
        && name
            .chars()
            .next()
            .is_some_and(|c| c.is_ascii_lowercase() || c.is_ascii_digit())
        && name
            .chars()
            .all(|c| c.is_ascii_lowercase() || c.is_ascii_digit() || c == '_')
}

/// 校验清单：id/name 字符集、版本、命令安全性、风险级别、超时范围。
pub fn validate_manifest(manifest: &PluginManifest) -> Result<(), String> {
    if !valid_plugin_id(&manifest.id) {
        return Err(format!("插件 id 非法：{}", manifest.id));
    }
    if !valid_tool_name(&manifest.name) {
        return Err(format!("工具名非法：{}", manifest.name));
    }
    if manifest.version.trim().is_empty() {
        return Err("缺少 version".into());
    }
    let Some(program) = manifest.command.first() else {
        return Err("缺少 command".into());
    };
    if program.is_empty()
        || program.contains('/')
        || program.contains('\\')
        || Path::new(program).is_absolute()
    {
        return Err(format!("command[0] 不允许路径分隔符或绝对路径：{program}"));
    }
    match manifest.risk_level.as_str() {
        "safe" | "moderate" | "dangerous" => {}
        other => return Err(format!("risk_level 非法：{other}")),
    }
    if let Some(secs) = manifest.timeout_secs {
        if !(1..=MAX_TIMEOUT_SECS).contains(&secs) {
            return Err(format!("timeout_secs 必须在 1..={MAX_TIMEOUT_SECS} 之间：{secs}"));
        }
    }
    Ok(())
}
```

`registry.rs` 测试块追加：

```rust
    #[test]
    fn remove_unregisters_tool() {
        let mut registry = ToolRegistry::new();
        registry.register(Arc::new(EchoTool));
        assert!(registry.remove("echo"));
        assert!(registry.all_schemas().is_empty());
        assert!(!registry.remove("echo"));
    }
```

`plugin.rs` 底部测试块：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use serde_json::json;

    fn manifest_json(id: &str, name: &str, command: &[&str]) -> String {
        json!({
            "id": id,
            "name": name,
            "display_name": name,
            "author": "test",
            "version": "1.0.0",
            "description": "test tool",
            "command": command,
        })
        .to_string()
    }

    #[test]
    fn parse_accepts_valid_manifest_with_defaults() {
        let manifest =
            parse_manifest(&manifest_json("demo-tool", "demo_upper", &["python", "main.py"]))
                .expect("合法清单");
        assert_eq!(manifest.risk_level, "moderate");
        assert_eq!(manifest.timeout_secs, None);
        assert_eq!(manifest.parameters["type"], "object");
    }

    #[test]
    fn parse_rejects_bad_id_or_name() {
        assert!(parse_manifest(&manifest_json("../evil", "demo", &["python"])).is_err());
        assert!(parse_manifest(&manifest_json("demo", "Bad-Name", &["python"])).is_err());
        assert!(parse_manifest(&manifest_json("demo", "1demo", &["python"])).is_ok());
        assert!(parse_manifest(&manifest_json("demo", "", &["python"])).is_err());
    }

    #[test]
    fn parse_rejects_path_in_command() {
        assert!(parse_manifest(&manifest_json("demo", "demo", &["C:/evil.exe"])).is_err());
        assert!(parse_manifest(&manifest_json("demo", "demo", &["..\\evil.exe"])).is_err());
        assert!(parse_manifest(&manifest_json("demo", "demo", &[])).is_err());
    }

    #[test]
    fn parse_rejects_bad_risk_level_and_timeout() {
        let bad_risk = json!({
            "id": "demo", "name": "demo", "version": "1.0.0",
            "command": ["python"], "risk_level": "extreme",
        })
        .to_string();
        assert!(parse_manifest(&bad_risk).is_err());
        let bad_timeout = json!({
            "id": "demo", "name": "demo", "version": "1.0.0",
            "command": ["python"], "timeout_secs": 9999,
        })
        .to_string();
        assert!(parse_manifest(&bad_timeout).is_err());
    }
}
```

- [ ] **步骤 2：跑测试确认编译失败**——`registry.remove` 未实现：

```powershell
cargo test -p lingxi-tools 2>&1 | Select-Object -Last 10
```
预期：编译错误 `no method named remove`。

- [ ] **步骤 3：实现 registry.remove**——`registry.rs` 的 `impl ToolRegistry` 中 `set_enabled` 之后插入：

```rust
    /// Unregister a tool by name (used when a plugin is uninstalled).
    /// Returns true if the tool was present.
    pub fn remove(&mut self, name: &str) -> bool {
        self.enabled.remove(name);
        self.tools.remove(name).is_some()
    }
```

- [ ] **步骤 4：Cargo.toml tokio 转正式依赖**（`command` 子进程执行需要）：

```toml
[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt", "time", "process", "io-util"] }
ureq = { version = "2", default-features = false, features = [
    "native-tls",
    "gzip",
    "json",
], optional = true }
native-tls = { version = "0.2", optional = true }
```
同时 `[features]` 的 net 改为 `net = ["dep:ureq", "dep:native-tls"]`（去掉 `dep:tokio`）。

- [ ] **步骤 5：lib.rs 导出模块**——`pub mod builtin;` 后加一行：

```rust
pub mod plugin;
```

- [ ] **步骤 6：跑测试确认通过**：

```powershell
cargo test -p lingxi-tools 2>&1 | Select-Object -Last 8
```
预期：`test result: ok`（plugin 4 个 + registry 4 个新测试全过）。

---

## Task 2：PluginTool 执行器 + 目录扫描

**Files:**
- Modify: `crates/tools/src/plugin.rs`（追加 PluginTool / scan / 执行测试）

- [ ] **步骤 1：写失败测试**——`plugin.rs` tests 块追加（Windows 实跑子进程）：

```rust
    #[cfg(windows)]
    #[tokio::test]
    async fn plugin_tool_runs_command_and_returns_stdout() {
        let manifest = parse_manifest(&manifest_json("demo", "demo_echo", &["cmd", "/c", "echo", "hello"]))
            .expect("合法清单");
        let tool = PluginTool {
            manifest,
            dir: std::env::temp_dir(),
        };
        let result = tool
            .execute(json!({"text": "ignored"}), &crate::context::ToolContext::auto_confirm("."))
            .await;
        assert!(result.success, "输出：{}", result.output);
        assert_eq!(result.output, "hello");
    }

    #[cfg(windows)]
    #[tokio::test]
    async fn plugin_tool_reports_nonzero_exit() {
        let manifest = parse_manifest(&manifest_json("demo", "demo_fail", &["cmd", "/c", "exit", "2"]))
            .expect("合法清单");
        let tool = PluginTool {
            manifest,
            dir: std::env::temp_dir(),
        };
        let result = tool.execute(json!({}), &crate::context::ToolContext::auto_confirm(".")).await;
        assert!(!result.success);
        assert!(result.output.contains("退出码"));
    }
```

- [ ] **步骤 2：跑测试确认编译失败**（`PluginTool` 不存在）：

```powershell
cargo test -p lingxi-tools 2>&1 | Select-Object -Last 6
```

- [ ] **步骤 3：实现 PluginTool**——`plugin.rs` 的 `validate_manifest` 之后追加：

```rust
/// 工具插件执行器：清单 + 插件目录。
pub struct PluginTool {
    manifest: PluginManifest,
    dir: PathBuf,
}

impl PluginTool {
    /// 从插件目录加载（读取并校验 tool.json）。
    pub fn load(dir: &Path) -> Result<Self, String> {
        let path = dir.join(MANIFEST_FILE);
        let text = std::fs::read_to_string(&path)
            .map_err(|error| format!("读取 tool.json 失败（{}）：{error}", path.display()))?;
        let manifest = parse_manifest(&text)?;
        Ok(Self {
            manifest,
            dir: dir.to_path_buf(),
        })
    }

    pub fn manifest(&self) -> &PluginManifest {
        &self.manifest
    }

    fn timeout(&self) -> Duration {
        let secs = self
            .manifest
            .timeout_secs
            .unwrap_or(DEFAULT_TIMEOUT_SECS)
            .clamp(1, MAX_TIMEOUT_SECS);
        Duration::from_secs(secs)
    }
}

#[async_trait]
impl Tool for PluginTool {
    fn schema(&self) -> ToolSchema {
        ToolSchema {
            name: self.manifest.name.clone(),
            description: format!(
                "[插件 {}] {}",
                self.manifest.display_name, self.manifest.description
            ),
            parameters: self.manifest.parameters.clone(),
        }
    }

    async fn execute(&self, params: serde_json::Value, _ctx: &crate::context::ToolContext) -> ToolResult {
        let mut command = tokio::process::Command::new(&self.manifest.command[0]);
        command
            .args(&self.manifest.command[1..])
            .current_dir(&self.dir)
            .stdin(std::process::Stdio::piped())
            .stdout(std::process::Stdio::piped())
            .stderr(std::process::Stdio::piped())
            .kill_on_drop(true);
        let mut child = match command.spawn() {
            Ok(child) => child,
            Err(error) => {
                return ToolResult::err(format!(
                    "启动插件命令失败（{}）：{error}",
                    self.manifest.command[0]
                ));
            }
        };
        // 参数 JSON 写入 stdin；写完立即关闭，脚本读到 EOF 即可解析。
        if let Some(mut stdin) = child.stdin.take() {
            use tokio::io::AsyncWriteExt;
            let payload = params.to_string();
            if let Err(error) = stdin.write_all(payload.as_bytes()).await {
                return ToolResult::err(format!("写入插件 stdin 失败：{error}"));
            }
            let _ = stdin.shutdown().await;
        }
        // 超时：Err 时 drop 掉持有 child 的 future，kill_on_drop 自动杀进程。
        let output = match tokio::time::timeout(self.timeout(), child.wait_with_output()).await {
            Ok(result) => match result {
                Ok(output) => output,
                Err(error) => return ToolResult::err(format!("等待插件进程失败：{error}")),
            },
            Err(_) => {
                return ToolResult::err(format!(
                    "插件执行超时（{} 秒）",
                    self.timeout().as_secs()
                ));
            }
        };
        let stdout = String::from_utf8_lossy(&output.stdout);
        if output.status.success() {
            ToolResult::ok(truncate_text(stdout.trim_end(), 64 * 1024))
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            ToolResult::err(format!(
                "插件退出码 {}：{}",
                output.status.code().unwrap_or(-1),
                truncate_text(stderr.trim_end(), 2048)
            ))
        }
    }

    fn risk_level(&self) -> RiskLevel {
        match self.manifest.risk_level.as_str() {
            "dangerous" => RiskLevel::Dangerous,
            "moderate" => RiskLevel::Moderate,
            _ => RiskLevel::Safe,
        }
    }
}

/// 按 UTF-8 字符边界截断，超长时附加截断标记。
fn truncate_text(text: String, max_bytes: usize) -> String {
    if text.len() <= max_bytes {
        return text;
    }
    let mut end = max_bytes;
    while end > 0 && !text.is_char_boundary(end) {
        end -= 1;
    }
    format!("{}\n…（输出已截断）", &text[..end])
}
```

- [ ] **步骤 4：实现扫描函数**——`plugin.rs` 末尾（tests 之前）追加：

```rust
/// 扫描插件根目录，返回全部可加载的工具插件；单个目录失败只记日志跳过。
/// 返回具体类型 Arc<PluginTool>，调用方注册时可自动 coerce 成 Arc<dyn Tool>，
/// 并能读取 manifest（构建 id→name 映射）。
pub fn scan_plugins(root: &Path) -> Vec<std::sync::Arc<PluginTool>> {
    let mut plugins = Vec::new();
    let Ok(entries) = std::fs::read_dir(root) else {
        return plugins;
    };
    for entry in entries.flatten() {
        let Ok(file_name) = entry.file_name().into_string() else {
            continue;
        };
        if !valid_plugin_id(&file_name) || !entry.path().is_dir() {
            continue;
        }
        match PluginTool::load(&entry.path()) {
            Ok(plugin) => plugins.push(std::sync::Arc::new(plugin)),
            Err(error) => eprintln!("[lingxi] plugin: 跳过无效插件 {file_name}：{error}"),
        }
    }
    plugins.sort_by(|a, b| a.manifest().name.cmp(&b.manifest().name));
    plugins
}
```

- [ ] **步骤 5：跑测试确认通过**：

```powershell
cargo test -p lingxi-tools 2>&1 | Select-Object -Last 8
```
预期：`test result: ok`（含 2 个 Windows 执行测试：echo 成功路径、exit 2 失败路径）。

---

## Task 3：overlay 状态与启动装配

**Files:**
- Modify: `apps/overlay/src/state.rs`（plugin_tool_map 字段）
- Modify: `apps/overlay/src/market.rs`（plugins_root + sync_plugin_tools，先放此处供 state/main 调用）
- Modify: `apps/overlay/src/main.rs`（setup 同步 + 命令注册占位）
- Modify: `apps/overlay/src/agent.rs`（ToolView.source + agent_chat 装配插件）

- [ ] **步骤 1：state.rs 加字段**——`AppState` 的 `lens_image` 之前插入：

```rust
    /// 已注册进 tool_registry 的插件工具：插件 id → 工具名。
    /// 卸载/同步时按值反向移除注册表项；list_tools 据此标注 source。
    pub(crate) plugin_tool_map:
        std::sync::Mutex<std::collections::HashMap<String, String>>,
```
`Default::default()` 的 `lens_image` 之前插入：

```rust
            plugin_tool_map: std::sync::Mutex::new(Default::default()),
```

- [ ] **步骤 2：market.rs 加 plugins_root 与 sync_plugin_tools**——`builtin_catalog_path` 之后插入：

```rust
/// 插件根目录：%APPDATA%/lingxi/plugins（工具插件下载落位点）。
pub fn plugins_root() -> Option<PathBuf> {
    dirs::config_dir().map(|dir| dir.join("lingxi").join("plugins"))
}

/// 全量同步插件工具到注册表：按 plugin_tool_map 移除旧项 → 重扫目录注册 →
/// 重建映射。启动与市场安装/卸载工具后各调一次；目录不存在时等价于清空。
/// 锁序恒为 tool_registry → plugin_tool_map，与他处一致。
pub(crate) fn sync_plugin_tools(state: &AppState) {
    let root = plugins_root().unwrap_or_else(|| PathBuf::from("."));
    let mut registry = state.tool_registry.safe_lock();
    for name in state.plugin_tool_map.safe_lock().values() {
        registry.remove(name);
    }
    let mut map = std::collections::HashMap::new();
    for plugin in lingxi_tools::plugin::scan_plugins(&root) {
        map.insert(plugin.manifest().id.clone(), plugin.manifest().name.clone());
        registry.register(plugin);
    }
    *state.plugin_tool_map.safe_lock() = map;
}
```
（`registry.register(plugin)` 中 `Arc<PluginTool>` 自动 coerce 为 `Arc<dyn Tool>`。）

- [ ] **步骤 3：main.rs setup 装配**——`let state = app.state::<AppState>();` 之后插入：

```rust
            // 启动即扫描用户插件目录，工具插件随内置工具一并注册。
            market::sync_plugin_tools(&state);
```

- [ ] **步骤 4：agent.rs——ToolView 加 source**：

```rust
#[derive(Serialize)]
pub(crate) struct ToolView {
    pub(crate) name: String,
    pub(crate) description: String,
    pub(crate) risk_level: String,
    pub(crate) enabled: bool,
    /// "builtin"（内置工具）| "plugin"（市场安装的插件工具）。
    pub(crate) source: String,
}
```
`list_tools` 改为：

```rust
#[tauri::command]
pub(crate) fn list_tools(state: State<AppState>) -> Vec<ToolView> {
    let reg = state.tool_registry.safe_lock();
    let plugin_names: Vec<String> =
        state.plugin_tool_map.safe_lock().values().cloned().collect();
    reg.all_schemas()
        .iter()
        .map(|s| {
            let risk = reg.risk_of(&s.name).unwrap_or(RiskLevel::Safe);
            ToolView {
                name: s.name.clone(),
                description: s.description.clone(),
                risk_level: format!("{:?}", risk).to_lowercase(),
                enabled: reg.is_enabled(&s.name),
                source: if plugin_names.contains(&s.name) {
                    "plugin".into()
                } else {
                    "builtin".into()
                },
            }
        })
        .collect()
}
```

- [ ] **步骤 5：agent.rs——agent_chat 重建注册表时纳入插件**——`lingxi_tools_windows::register_default_tools(&mut registry);`（agent_chat 内那处）之后插入：

```rust
    // 工具插件与内置工具同权参与本轮对话（enabled 状态由下方循环统一复制）。
    if let Some(root) = crate::market::plugins_root() {
        for plugin in lingxi_tools::plugin::scan_plugins(&root) {
            registry.register(plugin);
        }
    }
```

- [ ] **步骤 6：编译验证**（overlay 独立 lock，须在其目录）：

```powershell
cd apps\overlay; cargo build 2>&1 | Select-Object -Last 5
```
预期：`Finished` 无错误。

---

## Task 4：market.rs kind 全链路（模型/校验/安装/卸载/清单命令）

**Files:**
- Modify: `apps/overlay/src/market.rs`
- Modify: `apps/overlay/src/main.rs`（注册 list_tool_plugins）

- [ ] **步骤 1：写失败测试**——tests 块 `demo_entry()` 补 `kind: "skin".into(),`，并追加：

```rust
    #[test]
    fn sanitize_rejects_unknown_kind() {
        assert!(sanitize_entry(MarketEntry { kind: "widget".into(), ..demo_entry() }).is_none());
        assert!(sanitize_entry(MarketEntry { kind: "tool".into(), ..demo_entry() }).is_some());
        // 缺省 kind = skin（旧清单向后兼容）
        let legacy = r#"{"entries":[{"id":"s","name":"n","version":"1","url":"https://e.com/a.zip","sha256":"0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"}]}"#;
        let catalog = parse_catalog(legacy).expect("旧清单应兼容");
        assert_eq!(catalog.entries[0].kind, "skin");
    }
```

- [ ] **步骤 2：跑测试确认失败**（`MarketEntry` 无 kind 字段）：

```powershell
cargo test 2>&1 | Select-Object -Last 6
```

- [ ] **步骤 3：模型与校验**：
`MarketEntry` 的 `thumbnail` 字段后追加：

```rust
    /// 商品类型："skin"（皮肤包）| "tool"（工具插件）；缺省 skin。
    #[serde(default = "default_kind")]
    pub kind: String,
```
文件头部常量区后追加：

```rust
fn default_kind() -> String {
    "skin".into()
}
```
`MarketItem` 的 `updatable` 后追加：

```rust
    /// "skin" | "tool"。
    pub kind: String,
```
`sanitize_entry` 的 sha256 校验之后追加：

```rust
    if entry.kind != "skin" && entry.kind != "tool" {
        eprintln!("[lingxi] market: 丢弃未知 kind 的条目：{}（{}）", entry.id, entry.kind);
        return None;
    }
```

- [ ] **步骤 4：safe_extract 按 kind 放行脚本**——常量替换为：

```rust
/// 原生可执行内容：任何商品都禁止（不可审查、绕过签名体系）。
const NATIVE_BLOCKED_EXTS: [&str; 6] = ["exe", "dll", "vbs", "scr", "msi", "com"];
/// 脚本类型：皮肤禁止；工具插件放行（脚本是工具插件的交付物）。
const SCRIPT_BLOCKED_EXTS: [&str; 3] = ["bat", "cmd", "ps1"];
```
`safe_extract` 签名改 `fn safe_extract(zip_path: &Path, dest_dir: &Path, allow_scripts: bool)`，扩展名检查改为：

```rust
        if let Some(ext) = relative
            .extension()
            .and_then(|value| value.to_str())
            .map(|value| value.to_ascii_lowercase())
        {
            let native = NATIVE_BLOCKED_EXTS.contains(&ext.as_str());
            let script = SCRIPT_BLOCKED_EXTS.contains(&ext.as_str());
            if native || (script && !allow_scripts) {
                return Err(format!("压缩包内含禁止的文件类型：{ext}"));
            }
        }
```

- [ ] **步骤 5：flatten_single_root 参数化**——签名改：

```rust
fn flatten_single_root(dir: &Path, manifest_file: &str) -> Result<(), String> {
    if dir.join(manifest_file).is_file() {
        return Ok(());
    }
```
错误文案改 `format!("压缩包结构不符合要求（根部缺少 {manifest_file}）")`。

- [ ] **步骤 6：market_list 安装状态按 kind**——`installed_info` 后追加：

```rust
/// 工具插件安装状态：目录存在且清单合法即视为已安装（来源固定 user）。
fn installed_tool_info(id: &str) -> Option<(String, String)> {
    let dir = plugins_root()?.join(id);
    let plugin = lingxi_tools::plugin::PluginTool::load(&dir).ok()?;
    Some((
        pet_skin::SKIN_SOURCE_USER.to_string(),
        plugin.manifest().version.clone(),
    ))
}
```
`market_list` 循环中 `installed_info(...)` 一行替换为：

```rust
        let (installed_source, installed_version) = match entry.kind.as_str() {
            "tool" => installed_tool_info(&entry.id).unwrap_or_default(),
            _ => installed_info(&entry.id).unwrap_or_default(),
        };
```
`items.push(MarketItem {` 内补 `kind: entry.kind.clone(),`。

- [ ] **步骤 7：market_install 路由**——`let entry = ...ok_or_else(...)?;` 之后改造（逐段替换）：

```rust
    let is_tool = entry.kind == "tool";
    if !is_tool && pet_skin::skin_source(&id) == Some(pet_skin::SKIN_SOURCE_BUILTIN) {
        return Err("与内置皮肤冲突，无法安装".into());
    }
    let user_root = if is_tool {
        plugins_root().ok_or_else(|| "无法定位插件目录".to_string())?
    } else {
        pet_skin::user_skins_dir().ok_or_else(|| "无法定位用户皮肤目录".to_string())?
    };
```
解压调用改：

```rust
    if let Err(error) = safe_extract(&zip_path, &tmp_dir, is_tool) {
        cleanup(&tmp_dir);
        return Err(error);
    }
    let _ = std::fs::remove_file(&zip_path);
    if let Err(error) = flatten_single_root(&tmp_dir, if is_tool { "tool.json" } else { "skin.json" }) {
        cleanup(&tmp_dir);
        return Err(error);
    }
```
清单校验段（原 `load_manifest_at` 调用处）替换为：

```rust
    if is_tool {
        // 工具包：校验清单 + id 一致性 + 工具名冲突（内置或其他插件占用）。
        let plugin = lingxi_tools::plugin::PluginTool::load(&tmp_dir).map_err(|error| {
            cleanup(&tmp_dir);
            format!("工具包校验失败：{error}")
        })?;
        if plugin.manifest().id != id {
            cleanup(&tmp_dir);
            return Err(format!(
                "工具包目录与清单 id 不一致：目录 {id}，清单 {}",
                plugin.manifest().id
            ));
        }
        let name = plugin.manifest().name.clone();
        let conflict = {
            let registry = state.tool_registry.safe_lock();
            let exists = registry.all_schemas().iter().any(|s| s.name == name);
            let is_self = state
                .plugin_tool_map
                .safe_lock()
                .get(&id)
                .is_some_and(|old| *old == name);
            exists && !is_self
        };
        if conflict {
            cleanup(&tmp_dir);
            return Err(format!("工具名 {name} 已被占用（内置工具或其他插件）"));
        }
    } else if let Err(error) = pet_skin::load_manifest_at(&tmp_dir, &id) {
        cleanup(&tmp_dir);
        return Err(format!("皮肤包校验失败：{error}"));
    }
```
成功落地后（rename/备份清理完成之后），把末段广播改为按需，并追加工具同步：

```rust
    // 4) 皮肤的升级目标正在使用中 → 重载并广播；工具插件 → 同步注册表。
    if is_tool {
        sync_plugin_tools(state.inner());
        return Ok(());
    }
    let (is_current, overrides, visible) = { ... 原样保留 ... };
    ... 原样保留 ...
```

- [ ] **步骤 8：market_uninstall 路由**——`market_uninstall` 顶部（id 校验后）插入：

```rust
    // 先按皮肤查；不是皮肤再按工具插件查（工具没有「内置」形态）。
    let tool_dir = plugins_root().map(|root| root.join(&id));
    let is_tool = pet_skin::skin_source(&id).is_none()
        && tool_dir
            .as_ref()
            .is_some_and(|dir| dir.join(lingxi_tools::plugin::MANIFEST_FILE).is_file());
    if is_tool {
        let target = tool_dir.expect("已确认存在");
        let plugin = lingxi_tools::plugin::PluginTool::load(&target)
            .map_err(|error| format!("读取插件清单失败：{error}"))?;
        // 先摘注册表再删目录：删除失败时下次 sync 仍能找回。
        state
            .tool_registry
            .safe_lock()
            .remove(&plugin.manifest().name);
        state.plugin_tool_map.safe_lock().remove(&id);
        std::fs::remove_dir_all(&target)
            .map_err(|error| format!("删除插件目录失败：{error}"))?;
        return Ok(());
    }
```

- [ ] **步骤 9：list_tool_plugins 命令**——market.rs 追加：

```rust
/// 已安装工具插件列表（市场「已安装」页签）。
#[tauri::command]
pub(crate) fn list_tool_plugins() -> Vec<ToolPluginView> {
    let root = plugins_root().unwrap_or_else(|| PathBuf::from("."));
    lingxi_tools::plugin::scan_plugins(&root)
        .into_iter()
        .map(|plugin| {
            let manifest = plugin.manifest();
            ToolPluginView {
                id: manifest.id.clone(),
                name: manifest.name.clone(),
                display_name: if manifest.display_name.is_empty() {
                    manifest.name.clone()
                } else {
                    manifest.display_name.clone()
                },
                author: manifest.author.clone(),
                version: manifest.version.clone(),
                description: manifest.description.clone(),
            }
        })
        .collect()
}

/// 已安装工具插件的展示视图。
#[derive(Serialize)]
pub(crate) struct ToolPluginView {
    pub id: String,
    pub name: String,
    pub display_name: String,
    pub author: String,
    pub version: String,
    pub description: String,
}
```
`market_uninstall`/`market_install` 原有 `market_list` 命令上方的注释块同步更新为「皮肤与工具插件两类商品」。

- [ ] **步骤 10：main.rs 注册命令**——`market::market_uninstall,` 之后加：

```rust
            market::list_tool_plugins,
```

- [ ] **步骤 11：测试与编译**：

```powershell
cargo test 2>&1 | Select-Object -Last 12
```
预期：`test result: ok`（含新增 kind 测试；demo_entry 补字段后旧测试不受影响）。

---

## Task 5：前端（市场卡 kind 徽章 + 已安装工具列表 + 工具页插件标识）

**Files:**
- Modify: `apps/overlay/ui/app.js`
- Modify: `apps/overlay/ui/styles.css`

- [ ] **步骤 1：状态缓存**——`let marketBusyId = "";` 之后加：

```js
let toolPluginsCache = [];
```

- [ ] **步骤 2：loadMarket 拉取工具插件**——`Promise.all` 改为：

```js
    const [items, skins, config, tools] = await Promise.all([
      invoke("market_list"),
      invoke("list_pet_skins"),
      invoke("current_pet_config"),
      invoke("list_tool_plugins"),
    ]);
    marketCache = items;
    installedCache = skins;
    currentSkinId = config.skin.id;
    toolPluginsCache = tools;
```

- [ ] **步骤 3：已安装页签合并渲染**——`renderMarketView` 的 else 分支替换为：

```js
  } else {
    el.marketEmpty.hidden = true;
    const total = installedCache.length + toolPluginsCache.length;
    el.installedEmpty.hidden = total > 0;
    for (const skin of installedCache) el.marketGrid.appendChild(renderInstalledCard(skin));
    for (const plugin of toolPluginsCache) el.marketGrid.appendChild(renderToolPluginCard(plugin));
  }
```

- [ ] **步骤 4：市场卡显示类型**——`renderMarketCard` 的 meta 行改为：

```js
  meta.textContent = [item.kind === "tool" ? "工具插件" : "皮肤", item.author, item.version].filter(Boolean).join(" · ");
```

- [ ] **步骤 5：工具插件已安装卡 + 删除**——`renderInstalledCard` 之后追加：

```js
function renderToolPluginCard(plugin) {
  const card = document.createElement("div");
  card.className = "market-card";
  const thumb = document.createElement("div");
  thumb.className = "market-card-thumb is-tool";
  const name = document.createElement("span");
  name.className = "market-card-name";
  name.textContent = plugin.display_name || plugin.name;
  const meta = document.createElement("small");
  meta.className = "market-card-meta";
  meta.textContent = ["工具插件", plugin.author, plugin.version].filter(Boolean).join(" · ");
  const desc = document.createElement("p");
  desc.className = "market-card-desc";
  desc.textContent = plugin.description || "";
  const foot = document.createElement("div");
  foot.className = "market-card-foot";
  const del = document.createElement("button");
  del.className = "market-action danger";
  del.textContent = marketBusyId === plugin.id ? "删除中…" : "删除";
  del.disabled = Boolean(marketBusyId);
  del.addEventListener("click", () => uninstallPlugin(plugin));
  foot.append(del);
  card.append(thumb, name, meta, desc, foot);
  return card;
}

async function uninstallPlugin(plugin) {
  if (!invoke || marketBusyId) return;
  const label = plugin.display_name || plugin.name;
  if (!confirm(`确定删除工具插件「${label}」吗？此操作不可撤销。`)) return;
  marketBusyId = plugin.id;
  renderMarketView();
  try {
    await invoke("market_uninstall", { id: plugin.id });
    showStatus("已删除「" + label + "」", "ok");
  } catch (e) {
    showStatus("删除失败: " + e, "err");
  } finally {
    marketBusyId = "";
    await loadMarket();
  }
}
```

- [ ] **步骤 6：工具页插件标识**——`renderTools` 中 `head.append(icon, name);` 之后插入：

```js
    if (t.source === "plugin") {
      const tag = document.createElement("span");
      tag.className = "tool-card-plugin";
      tag.textContent = "插件";
      head.append(tag);
    }
```

- [ ] **步骤 7：styles.css 末尾追加**：

```css
/* ---- 工具插件（市场/工具页标识） ---- */
.market-card-thumb.is-tool {
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--accent, #6366f1), #8b5cf6);
  color: rgba(255, 255, 255, 0.9);
  font-size: 10px;
  letter-spacing: 1px;
}
.market-card-thumb.is-tool::after {
  content: "TOOL";
}
.tool-card-plugin {
  font-size: 10px;
  line-height: 1;
  padding: 3px 6px;
  border-radius: 999px;
  background: color-mix(in srgb, var(--accent, #6366f1) 18%, transparent);
  color: var(--accent, #6366f1);
  border: 1px solid color-mix(in srgb, var(--accent, #6366f1) 40%, transparent);
  flex: none;
}
```

- [ ] **步骤 8：磁盘复核 + 重建**（前端资源嵌入 exe）：

```powershell
Select-String -Path .\ui\app.js -Pattern "toolPluginsCache|renderToolPluginCard|uninstallPlugin" | Measure-Object -Line
cargo build 2>&1 | Select-Object -Last 4
```
预期：行数 ≥ 5；`Finished`。

---

## Task 6：演示工具包 + 内置清单条目

**Files:**
- Create: `apps/overlay/target/market-demo/lingxi-tool-upper/tool.json`
- Create: `apps/overlay/target/market-demo/lingxi-tool-upper/main.py`
- Create: `apps/overlay/target/market-demo/lingxi-tool-upper-1.0.0.zip`（打包生成）
- Modify: `apps/overlay/ui/assets/market/market.json`（新增工具条目）

- [ ] **步骤 1：tool.json**（Write 工具写，UTF-8 无 BOM）：

```json
{
  "id": "lingxi-tool-upper",
  "name": "upper_text",
  "display_name": "文本大写",
  "author": "lingxi",
  "version": "1.0.0",
  "description": "演示工具插件：把输入文本转成大写字母（python 脚本实现）",
  "risk_level": "safe",
  "command": ["python", "main.py"],
  "parameters": {
    "type": "object",
    "properties": {
      "text": { "type": "string", "description": "要转换的文本" }
    },
    "required": ["text"]
  },
  "timeout_secs": 15
}
```

- [ ] **步骤 2：main.py**：

```python
import json
import sys

try:
    data = json.load(sys.stdin)
    text = str(data.get("text", ""))
    print(text.upper())
except Exception as error:
    print(f"ERROR: {error}")
    sys.exit(1)
```

- [ ] **步骤 3：打包并取哈希**（保留顶层目录）：

```powershell
$zip = ".\target\market-demo\lingxi-tool-upper-1.0.0.zip"
if (Test-Path $zip) { Remove-Item $zip -Force }
Compress-Archive -Path ".\target\market-demo\lingxi-tool-upper" -DestinationPath $zip
"SIZE=" + (Get-Item $zip).Length
"SHA256=" + (Get-FileHash $zip -Algorithm SHA256).Hash
```

- [ ] **步骤 4：market.json 增加工具条目**——`entries` 数组追加（sha256/size 用步骤 3 实测值）：

```json
    {
      "id": "lingxi-tool-upper",
      "kind": "tool",
      "name": "文本大写",
      "author": "lingxi",
      "version": "1.0.0",
      "description": "演示工具插件：把输入文本转成大写字母（python 脚本实现）",
      "thumbnail": "",
      "url": "http://127.0.0.1:8787/lingxi-tool-upper-1.0.0.zip",
      "sha256": "<实测SHA256小写>",
      "size": <实测字节数>
    }
```

- [ ] **步骤 5：同步到服务器目录并复核**：

```powershell
Copy-Item ".\ui\assets\market\market.json" ".\target\market-demo\market.json" -Force
Select-String -Path ".\ui\assets\market\market.json" -Pattern '"kind": "tool"'
Invoke-WebRequest -Uri "http://127.0.0.1:8787/market.json" -UseBasicParsing | Select-Object -ExpandProperty StatusCode
```
预期：命中 1 行；返回 200。

---

## Task 7：端到端验收

- [ ] **步骤 1：重建重启**（若新 exe 被 WDAC 拦，重跑一次 build 换哈希）：

```powershell
Stop-Process -Name overlay -Force -ErrorAction SilentlyContinue
cargo build 2>&1 | Select-Object -Last 3
Start-Process -FilePath ".\target\debug\overlay.exe"
```

- [ ] **步骤 2：市场页验收**——设置 → 进入插件市场：
  - 「市场」页签出现两张卡：「仓鼠·夜色」（皮肤）与「文本大写」（工具插件，meta 行显示「工具插件 · lingxi · 1.0.0」）
  - 点「文本大写」的「安装」→ 下载中… → 完成后按钮变「已安装」

- [ ] **步骤 3：工具注册验收**——设置 → Agent 能力页：
  - 出现 `upper_text` 工具卡（描述前缀 `[插件 文本大写]`），右上角「插件」徽章，风险「安全」

- [ ] **步骤 4：Agent 调用验收**（需云端模型已配置）——Agent 对话输入：
  > 用 upper_text 工具把 "hello lingxi" 转成大写
  预期：模型调用 upper_text，返回 `HELLO LINGXI`。

- [ ] **步骤 5：卸载验收**——市场 → 已安装页签：
  - 「文本大写」卡（工具插件卡，紫色 TOOL 缩略图）→ 删除 → 确认 → 消失
  - 回 Agent 能力页：`upper_text` 工具卡消失

- [ ] **步骤 6：回归验收**（皮肤闭环不受影响）：
  - 「仓鼠·夜色」安装/删除流程照常
  - `cargo test`（apps/overlay）与 `cargo test -p lingxi-tools`（根目录）全绿

---

## 自检清单（Self-Review 结论）

- 方案覆盖：清单/安装/卸载/注册同步/前端/演示包/验收闭环全覆盖；旧 skin 清单无 kind 字段向后兼容（serde default + 测试覆盖）。
- 类型一致性：`plugin_tool_map`（id→name HashMap）在 state/market/agent 三处用法一致；`scan_plugins` 返回 `Arc<PluginTool>` 与 `registry.register(Arc<dyn Tool>)` 的 coercion 已验证可行。
- 已知取舍：插件 enabled 状态重启后回到默认开启（与内置工具现状一致）；不同插件 id 注册同名工具会被安装期冲突检查拒绝。
